# Planyfi App — Archive (2026-06)

- [x] Research available US datasets for household income, saving/investing, and spending — delivered themed HTML report at design-mockups/us-data-report.html
- [x] Home Purchase event summary renders `"$NaN home, undefined% down, undefined% rate, 3% appreciation"` in the Future Events drawer card. Seen on a fresh Quick Setup demo plan (seeded HOME_PURCHASE event) — home price / down payment / rate fields come through undefined when the event is auto-seeded rather than user-entered. Likely the seeded composite payload is missing fields the card summary reads, or the summary doesn't guard against undefined.
- [x] Require account creation to use the app — DONE 2026-06-15. `middleware.ts` now protects all page routes except `/sign-in`/`/sign-up` (+ public API routes); unauthenticated → `/sign-up`. `ProfileContext` flips `isSessionOnly`/`setSessionOnly` to key off `isGuest` (not `!isPremium`), so authed free users persist to the DB. Removed the "Continue without an account" sign-in/up bypass + the onboarding "Previous session cleared" / guest CTAs; `GuestSessionBanner` now early-returns for authed users. Guest store + api-client guest router kept dormant for the future demographics/comparison mode. Updated CLAUDE.md + Help FAQ + deployment-strategy memory. Verified tsc + production build; in-browser confirmed signed-out `/financial-planner` and `/onboarding` redirect to `/sign-up`. (Signed-in free-persistence + new-user onboarding-persists need an authed click-through.)
- [x] Free-tier persistence limit — DECIDED + BUILT (Phases 0-2), 2026-06-15, branch `scenario-comparison` (unmerged). Mechanism: **30-day read-only freeze** (never deletion), anchored to the user's first save. Free plans persist normally, then become view-only once the window elapses; upgrading lifts it instantly; nothing is ever deleted. **Phase 0** (data model): `profiles.firstSavedAt` (anchor, stamped on first budget/account/onboarding write) + `profiles.plan` (Clerk plan mirror); migration backfills `firstSavedAt = deploy time` for existing users with data so nobody freezes day one; `FREE_RETENTION_DAYS=30` in lib/constants.ts. **Phase 1** (server, authoritative): `lib/api/retention.ts` `assertNotFrozen` → 403 `retention_frozen` on 35 mutating handlers across 14 routes (reads stay open; profiles/onboarding/notifications/projected-balances excluded); plan mirror synced via the Clerk billing webhook + admin/set-plan, lazily backfilled from Clerk. **Phase 2** (client): `ProfileContext.retention`, `RetentionBanner` (dismissible countdown in last 14d + non-dismissible frozen "view-only/Upgrade"), global `fetchApi` 403 → upgrade modal + toast, `CurrentPlanDrawer` read-only ("View Only" badge + "Upgrade to edit"). Verified in browser both paths (non-frozen: loads + guarded `PUT /api/budgets 200`; frozen: banner + locked editor + 403 + modal/toast). Phase 3 + rollout follow-ups below.
- [x] Free-tier retention freeze — Phase 3 (notice + export) — DONE 2026-06-17. The two substantive parts (in-app notifications + free JSON export) and the read-only drawer polish all shipped. Deferred from the Phases 0-2 build above. (1) [x] **In-app notifications** at T-14 / T-7 / T-1 and on freeze — DONE 2026-06-17: `/api/notifications/generate` now reuses `getRetentionState` and emits one notification per milestone (2 weeks / 1 week / final day / frozen), deduped by a stable `referenceId` (`retention_14/7/1/frozen`, regardless of read/dismissed so no re-nagging); clears all `referenceType='retention'` notices on upgrade. New `retention_freeze` NotificationType/`retention` referenceType; NotificationBell's action fires the upgrade modal (`show-upgrade-modal`) instead of a route. Messages also point users to the free "Download my data" export. Verified tsc + production build. (2) [x] **Free "Download my data" (JSON) export** button — DONE 2026-06-17: `GET /api/user-data` (paired with the existing DELETE wipe) exports the user's whole dataset (profile + all per-user tables, child tables scoped via account/scenario subqueries, defensive `safeAll`) as a downloadable JSON; read-only so it stays open even when frozen. ProfileDrawer "Download my data" button (always available, emphasized accent style in the last 14 days / after freeze). (3) **Email reminders** at T-7 + on freeze — needs a transactional email provider; optional. (4) [x] **Per-drawer read-only polish** — DONE 2026-06-17: shared `app/components/RetentionLock.tsx` (`useRetentionLock` + `RetentionLockBadge`/`RetentionLockBanner` + `openUpgradeModal`); applied "View Only" badge + frozen banner + disabled (pointer-events-none) editor body to AccountsDrawer, AllocationStrategyDrawer/Fund Strategy, and TimelineDrawer. Mirrors the CurrentPlanDrawer lock. Verified tsc + production build.
- [x] Guest-flow dead-code cleanup — DONE 2026-06-17 (DECISION: keep dormant for the future no-account mode, annotate + isolate; no deletion). (1) Added clear "DORMANT" file/section headers correcting the stale pre-pivot model wording. (2) Replaced GuestSessionBanner's blocking `confirm()` "Start over" with a non-blocking inline confirm (Clear / Cancel). (3) **Live bug fixed:** ProfileDrawer's Sign Out showed authenticated *free* users a "your data will be permanently deleted" warning and `handleFreeSignOut` actually `DELETE`d their persisted data on sign-out — guest-era behavior that contradicts the post-pivot model. Sign Out now just signs out for all authenticated users. Verified tsc + production build.
- [x] a11y: add aria-labels to icon-only buttons — added `aria-label`/`title` to the shared Drawer.tsx close X and every custom-header drawer close button (CurrentPlan, Accounts, Transactions, ProjectedPlan, Timeline ×2, AllocationStrategy); AccountSetupWizard close X + duplicate/remove buttons now carry descriptive aria-labels (e.g. "Remove Checking"). Verified tsc + production build (/qa Jun 2026)
- [x] Surface silent save states: AccountsContent bulk save has no in-flight indicator; onboarding `void updateProfile(...)` swallows errors; NotificationBell shows "No notifications" during initial fetch — add loading state (/qa Jun 2026)
- [x] ScenarioManagerBar name input commits onUpdate per keystroke → new comparisonScenarios array → full recharts LineChart + ComparisonTable rebuild on every character (ScenarioManagerBar.tsx:127; projection engine is cache-shielded but all downstream UI work repeats). Use local draft state committed on blur/Enter (pattern: ControlledNumberInput)
- [x] useComparisonProjections ref-cache pins full ScenarioProjectionResult incl. monthlyData (~360 points × per-account record maps per scenario) that no Compare consumer reads — strip monthlyData before cache.entries.set (useComparisonProjections.ts:139)
- [x] ComparisonChart duplicates ScenarioChart code verbatim: nice-domain/tick math (ComparisonChart.tsx:87–96 = ScenarioChart.tsx:232–241, repeated again at :258–267) and the click-to-select-year mousedown effect (`.recharts-cartesian-grid` query — 3rd dependence counting ChartAxisStrips.tsx:48). Extract shared niceDomain() helper + useChartYearClick() hook
- [x] AccountSetupWizard hardcodes its own account-type taxonomy — now derives GROUPS from ACCOUNT_TYPES/TYPE_DEFAULTS via a UI_GROUPS map (4 UI buckets, the two debt account-groups collapse into one "Debt"; 'Custom' excluded as it needs the full form), so any new account type auto-surfaces. Replaced the inline Unicode-minus `money()` formatter with shared `fmt` from lib/utils/format.ts. Verified tsc + production build
- [x] Build "Luis" Claude skill/agent as in-app design consultant for Planyfi — interviews Mitch on design preferences and Planyfi design system; outputs 3 HTML options per request to a designated folder
- [x] Surface silent background-load failures — cash-flow settings + overrides loads (financial-planner/page.tsx:408,434), Premium saved comparison scenarios hydration (page.tsx:1129 — also no loading indicator during the per-scenario resolve loop), and the PlanEditor accounts fetch (plan/edit/page.tsx:563) all swallow errors to console.error with no user feedback. Add useToast error surfacing.
- [x] Add saving/disabled state to async submit forms — AddAccountModal submit (AccountsContent.tsx:148) and household MemberForm submit (ProfileDrawer.tsx:681) run awaited DB writes but don't disable or show loading during the write → double-submit risk. Adopt the app's isSaving + "Saving…" pattern.
- [x] Standardize modal close affordances — AddAccountModal (AccountsContent.tsx:79) lacks Escape + backdrop-close + X; LinkAssetsModal (LinkAssetsModal.tsx:75) lacks backdrop-close + X (has Esc); UpgradePromptModal's guest variant (UpgradePromptModal.tsx:68) lacks backdrop-close + X even though the free-auth variant of the same component renders an X. Align all three with the standard modal (Esc + backdrop + X). — DONE 2026-06-24 (commit 70a1b39, branch worktree-qa-modal-cashflow-polish — unpushed/undeployed).
- [x] Standardize primary-button loading/validation — "Update Balance" (AccountEditModal.tsx:692) and the "This Transaction" Save (TransactionEditModal.tsx:423) run async/commit with no disabled/loading state; "Confirm Links" (LinkAssetsModal.tsx:160) has no validation gate. — DONE 2026-06-24 (commit 70a1b39, same branch).
- [x] Add an empty state to CashFlowSettingsDrawer (CashFlowSettingsDrawer.tsx:178) — when the user has no checking/savings/credit-card accounts every section is conditionally hidden and the drawer renders only the threshold input over blank space; add "add a cash account" guidance. — DONE 2026-06-24 (commit 70a1b39, same branch).
- [x] a11y gaps — MarketAssumptions trigger (MarketAssumptionsPopover.tsx:161) gates its aria-label on `compact`, but the non-compact usage hides the text label below sm: → an icon-only button with no accessible name on mobile; the inline sub-category rename input (plan/edit/page.tsx:3427) has no label/aria-label/placeholder; the clickable chart strip div (ChartAxisStrips.tsx:328) has no role/tabIndex/onKeyDown. — DONE 2026-06-26.
- [x] Async-submit guards (double-submit / no in-flight feedback) — disable + "Saving…" during the awaited write on: TransactionsDrawer transaction form (TransactionsDrawer.tsx:545) + Import button (:339); ProfileDrawer MemberForm submit (ProfileDrawer.tsx:681). — DONE 2026-06-26.
- [x] Event-form + wizard validation gates never block save — RetirementForm.tsx:66 + ChildBirthForm.tsx:55 isValid() hardcoded `return true`; PlanSetupWizard.tsx:371 Next/Continue never disabled + no minimum-selection gate. — DONE 2026-06-26.
- [x] HomePurchaseForm "Equivalent Rent Breakdown" modal (HomePurchaseForm.tsx:551) has backdrop-click + X close but no Escape-to-close — align with EventMatrixModal / TransactionEditModal. — DONE 2026-06-26.
- [x] a11y — icon-only buttons/toggles missing accessible name or role/state: modal close X's on TransactionEditModal, HomePurchaseForm rent-breakdown, PlanSetupWizard, AccountEditModal; TimelineDrawer add/edit-mode Back button; AllocationStrategyDrawer Roth enable toggle + year-override remove X; AllocationSummaryBox custom drift-threshold input; rent-vs-buy Time Horizon slider. — DONE 2026-06-26.
- [x] Recharts ResponsiveContainer renders at width(-1)/height(-1) → 18+ repeated console warnings on /financial-planner load. Add minHeight/minWidth or gate render on container visibility. — DONE 2026-06-26.
- [x] Dead hover states in BillCalendarTable — primary "Add Upcoming Transaction" CTA hover bg == base bg; "Reset override" icon hover color == base. — DONE 2026-06-26.
- [x] a11y — individual chart event/milestone markers are non-interactive `<div onClick>`s (ChartAxisStrips.tsx EventMarkerCard ~:458–464): no role/tabIndex/aria-label/keyboard handler. Make each marker a real button with accessible name and Enter/Space activation. — DONE 2026-06-27.
- [x] Integrate US census data more deeply into app — DONE 2026-06-26 (major pieces shipped). The demographic benchmark system (B0–B4) integrates static BLS/Census data via a pure resolver that auto-derives age from birthDate, surfaced as comparison badges + a net-worth-impact simulator on the Current Plan, the onboarding Demographics percentile table, and the household-size expense rescaling. Closing this umbrella item as substantively delivered.
- [x] Scenario Comparison feature: ability to hide/adjust Current Plan, Events, Milestones, Market Assumptions; full plan summary/net worth for a given year; line chart plotting net worth, income, expenses for up to 3 scenarios — DONE 2026-06-26. Delivered: Compare mode with up to 3 scenarios, per-scenario adjustable Current Plan / Events / Market Assumptions, the ComparisonTable, and the ComparisonChart line plot. Architecture sits on `scenarioProjectionCore` + membership-based save (Stages 1–6, all done). Merged into master 2026-06-19 behind beta gating.
- [x] Review Fund Strategy and Roth Conversion Ladder — verify accuracy, defaults, and quality of explanations. /qa Jun 2026: fixed Roth target-bracket text hardcoding MFJ figures (now uses plan's filing status), fixed deficit-withdraw "Available" totals double-counting overlapping accounts, updated drawer subtitle to mention Roth conversions.
- [x] Add insights to Current Plan page: hover/tap to see typical US household spend by category with user comparison; show impact of reducing expenses on net worth — comparison badges (CategoryBenchmarkBadge + DemographicBenchmarkContext) were already shipped. NEW: completed the scaffolded net-worth impact (Deliverable B3) — `lib/hooks/useBenchmarkImpact.ts` runs the pure projection core once per over-benchmark expense category, diffs terminal net worth vs. the already-computed baseline, and publishes a per-category map. Verified tsc + jest + production build.
- [x] Current Plan insights — contributions side — DONE (verified via /next 2026-06-26). The contribution benchmark COMPARISON already shipped 2026-06-22 as the section-level `SectionBenchmarkBadge kind="investments"`. DECISION (owner): the net-worth-impact half is DROPPED.
- [x] Consult Claude on improving account-adding UX; review full onboarding flow in separate project chat — review delivered (design-mockups/account-onboarding-ux-review/, 12 observations).
- [x] Show warning popup if non-premium user tries to refresh page — added a beforeunload handler in ProfileContext for session-only (non-premium) users that fires the native leave-site prompt only when the guest store has data. Verified in-browser via synthetic beforeunload dispatch.
- [x] Empty-state CTAs: planner "No projection data" panel and Allocation tab "Add holdings" message are dead ends — add buttons opening the Current Plan / Accounts / holdings flows. Verified tsc + production build (/qa Jun 2026)
- [x] Compare mode polish: identical scenario line exactly hides the Base line (consider dash/offset for Base); first added scenario is named "Scenario 2" (prev.length+2 in financial-planner/page.tsx — confirm intent or fix); ComparisonTable lacks an empty state when no scenarios are selected and shows bare "—" for out-of-range years; scenario chip calendar badge counts events only while the drawer also lists milestones (/qa Jun 2026)
- [x] Accounts drawer net-worth mini panel: asset-class sub-row labels truncate to invisible and the liabilities amount clips at the panel edge — root cause was the drawer's summary sidebar pinned to `w-48` at the 1200–1450px breakpoint. Widened the sidebar to `w-60`. Also bumped ScenarioSummaryBox's `AMT_COL` 78→88px. Verified tsc + production build (/qa Jun 2026)
- [x] Fund Strategy: "Mo. Expenses" bucket input renders value + "mo" suffix cramped ("6mo" reads as "6no") — fixed in shared ControlledNumberInput: the suffixed input had a fixed `paddingRight: 28px` (sized for the 1-char "%") so the value butted against multi-char suffixes. Now scales as `suffix.length * 7 + 21`px. Verified tsc + production build (/qa Jun 2026)
- [x] Add explanatory text at top of each onboarding page where missing — audited all Quick + Full path steps. Added the genuinely-missing "Your Accounts" heading + explanation to the **full-path Accounts step**. Rewrote the **Household & Income** subtitle. Broadened the **Expenses & Savings** subtitle. Verified tsc + production build.
- [x] On accounts page (post-account-add in detailed onboarding): add explanation that user can add underlying holdings; alternatively consider skipping this page given new onboarding account-add flow — DECISION: skip the page entirely. OnboardingAccountsWrapper rewritten to render AccountSetupWizard inline (embedded/isOnboarding). Verified tsc + production build
- [x] Add default sub-categories when users expand a category — e.g. expanding Transportation shows Car Payment, Auto Insurance, Gas. DONE 2026-06-19. Two changes in `app/plan/edit/page.tsx`: (1) Expanded `SUBCATEGORY_SUGGESTIONS` to cover every `DEFAULT_EXPENSE_CATEGORIES` name. (2) Rewrote `expandIntoSubCategories`: when a flat category has presets and no existing subs, it now SEEDS the suggested sub-category rows ($0 each). Verified tsc + production build.
- [x] Require account creation to use the app — DONE 2026-06-15 (see the implemented entry under Engineering → "Free tier / monetization"): account now required, free tier persists to the DB, guest flow retired/dormant.
- [x] Remove Split option for investment and savings — use separate contribution entries per account instead; same name allowed (e.g. Roth IRA) with owner tag to differentiate — DONE 2026-06-26 (uncommitted/undeployed). Section now maps `investmentSavingsItems` one-row-per-entry (index-keyed) instead of by unique category name. Verified tsc + 1284 financial tests pass + LIVE in browser.
- [x] If auto-showing Credit Card in debt section, add info icon or asterisk clarifying it only applies if carrying a balance beyond the statement balance — DONE 2026-06-26 (already implemented). `DEBT_CATEGORY_NOTES` (plan/edit/page.tsx:154) carries the Credit Card clarification, rendered as an info-icon `Tooltip`.
- [x] Add similar info icon/asterisk to Auto Loan in debt section — note it's typically already captured under Transportation as a car payment — DONE 2026-06-26 (already implemented). Same `DEBT_CATEGORY_NOTES` map carries the Auto Loan note.
- [x] Account-group label casing inconsistent across panels — aligned on sentence-case. Hoisted shared `ACCOUNT_GROUP_LABELS` map in AccountsContent.tsx.
- [x] Per-row action icons differ in color — added shared `ROW_ACTION_ICON` / `ROW_ACTION_ICON_DANGER` constants (brand-classes.ts) documenting the convention: resting = subtle gray, neutral hover = muted, destructive hover = red.
- [x] Top tab-bar status indicators mix metaphors — formalized convention: count/info = badge, customized/attention = dot. Market Assumptions dot was a hardcoded hex (#34d399); switched to `var(--color-accent)` token.
- [x] Plan Summary Waterfall "Expenses" row defaults expanded even with no child rows — ExpRow now detects real children (React.Children); when empty it hides the chevron and renders a non-interactive (non-toggling) row.
- [x] Onboarding benchmark table (DemographicsStep) forces horizontal scroll on phones — added a mobile-only (`sm:hidden`) stacked layout: one selectable card per percentile with a 2-col label/value breakdown. Desktop table now gated `hidden sm:block`.
- [x] Credit-card editor Field uses fixed w-40 (160px) input on mobile — made fluid: input wrapper now `flex-1 min-w-0 max-w-[160px]` on mobile (fills the row up to 160px, shrinks on 320px screens).
- [x] Disclose Quick Setup seeded events — added an `isSuggested` flag on events (lib/types/scenario.ts), set on seeded impact events. Timeline event cards now show an amber "Suggested" pill that dismisses on click; editing an event also clears it. Test added. Verified tsc + production build (/qa Jun 2026)
- [x] Onboarding polish: (1) default income percentile is now 50th (was 90th) — both the initial state and the path-reset; (2) benchmark expenses rescale when household composition changes on the Household step. New `impliedFamilySize`/`familyExpenseFactor` helpers. Verified tsc + jest (254) + production build (/qa Jun 2026)
- [x] Event conflict warning — a later SET-type event silently overrides an earlier one; surface a conflict hint in the event form/timeline (/qa Jun 2026). DONE: new pure util `lib/utils/eventConflicts.ts`. TimelineDrawer shows an amber "Overridden" pill. CustomEventForm shows an inline amber warning. 16 unit tests added. Verified tsc + jest.
- [x] Align save tiering copy — reframed the guest Save modal (UpgradePromptModal) to the persistence-gated model. Verified tsc (/qa Jun 2026)
- [x] Compare polish batch — all four addressed: (1) chip-row crowding: MarketAssumptionsPopover gained a `compact` icon-only mode; (2) "+ Add scenario" 3-cap now shows an inline "Max 3 scenarios" pill; (3) rail % basis labeled; (4) net-worth tooltip debts: ScenarioChartTooltip now groups assets above liabilities with a divider, and liabilities draw from a new red LIABILITY_PALETTE. Verified tsc + jest (/qa Jun 2026)
- [x] Compare: FI year / retirement age row at the top of ComparisonTable — added a "FI reached" row directly under Net Worth showing the FI year + each non-dependent member's age at FI, or "Not reached". Verified tsc + jest (/qa Jun 2026)
- [x] [M] Two segmented-toggle styles for the same control — the Year/Month period toggle in ScenarioSummaryBox + ComparisonTable is a bespoke `h-6 rounded-md` track, diverging from the canonical shared `SegmentedToggle`. Fix: route through the shared toggle classes. DONE 2026-06-17: extracted the canonical SegmentedToggle out of ChartControlsBar into a shared `app/components/ui/SegmentedToggle.tsx`. Verified tsc.
- [x] [M] Hero number uses the wrong accent role — the big Net Worth figure in ScenarioSummaryBox is `--color-accent`, while the Savings-rate hero and FI hero correctly use `--color-accent-display`. DONE 2026-06-17: net-worth hero now uses `--color-accent-display`. Also routed ComparisonTable's Net Worth + Remaining headline figures' positive branch to accent-display. Verified tsc.
- [x] [M] Mixed premium-gate metaphor on the same bar — CSV-export gate uses a lock icon + tooltip; the Save-scenarios gate uses a trailing "★" text glyph. DONE 2026-06-17: replaced the Save-scenarios gate's trailing "★" glyph with the same lock icon. Verified tsc.
- [x] [M] FI panel hardcodes rgba that half-disappears in light theme — the FI section uses `rgba(255,255,255,0.07)` for the progress-bar track + divider, and `rgba(52,211,153,0.22)` for the border. DONE 2026-06-17: rgba literals swapped to tokens. Verified tsc.
- [x] [S] Two voices for the "nothing to compare" empty state — ComparisonChart shows "Add a scenario to compare projections." at `p-8`; ComparisonTable shows "No scenarios to compare" + subtext at `p-6`. DONE 2026-06-17: both aligned to the same heading + subtext and `p-8` padding. Verified tsc.
- [x] [S] Chip action icons are hand-rolled — the calendar/checklist/remove icons in scenario chips use bespoke `h-6 w-6 rounded-md` instead of the shared icon-button family. DONE 2026-06-17: added compact 24px icon-button variants (`ICON_BTN_GHOST_SM`/`ICON_BTN_ACTIVE_SM`/`ICON_BTN_DANGER_SM`, all `rounded-[7px]`) to brand-classes. Verified tsc.
- [x] [S] ProfileDrawer var() fallback hex is wrong + needless — `var(--color-warning-bg,#2d1f00)` fallback doesn't match the real token. DONE 2026-06-17: dropped the dead `var()` fallback hexes. Verified tsc.
- [x] [S] Fund Strategy shared-class sweep — DONE 2026-06-23. Swept hand-rolled classes onto the shared `brand-classes.ts` constants across the Fund Strategy drawer + Allocation Summary rail. tsc clean. (/luis 2026-06-22)
  - [x] Roth "Conversion Mode" → shared `TOGGLE_TRACK_CLASS`/`TOGGLE_ACTIVE_CLASS`/`TOGGLE_INACTIVE_CLASS` — active is now a solid emerald fill.
  - [x] Footer "Done" → `BTN_PRIMARY_CLASS` (9px, bold); Roth "Add" → `rounded-[9px]`.
  - [x] "Edit Target Allocation" → accent text + `rounded-[9px]` + `hover:opacity-90`; "Fund Strategy" button → `rounded-[9px]`.
  - [x] Numeric inputs → `NUMERIC_CELL_CLASS`: cap/floor in BOTH `AllocationPriorityList` + `DeficitPriorityList`, and the drift input.
  - [x] Labels: summary-box eyebrows (5, incl. Rebalancing) → `FIELD_LABEL_CLASS`; drawer field labels (3) → `LABEL_CLASS`.
  - [x] Added `BADGE_COMPACT` to brand-classes; buy/sell + reduce/withdraw badges all use it. Also swept the green "+ Add Priority" / "Customize order" links → `accent-text`.
- [x] [M] CSS vars piped into SVG attributes don't resolve — `theme.ts:85` + the new `FIProjectionChart` both document it, yet `ScenarioChart` + `ComparisonChart` still pass `var(--color-chart-grid)` / `var(--color-chart-axis)` straight into `CartesianGrid` stroke, `YAxis`/`XAxis` tick fill, and `ReferenceLine`/cursor stroke. Fix: extract a shared `resolveChartColors(isDark)` → literal theme hex. `ScenarioChart.tsx`, `ComparisonChart.tsx`, `theme.ts` (/luis 2026-06-24)
- [x] [S–M] Two different tooltips on the same surface — the Net Worth tooltip uses `themeConfig.chart.tooltip.bgClass` while the Compare tooltip uses `themeConfig.surface.cardClass` + `shadow-lg`. Fix: one shared tooltip shell + one swatch style. (/luis 2026-06-24)
- [x] [S] Axis-tick font-size drifts across the three charts — ScenarioChart Y `12`, ComparisonChart X `11` / Y `12`, FIProjectionChart X `10`; design-system §8 says 11px. Fix: standardize on 11 via the theme chart config. (/luis 2026-06-24)
- [x] [S] Swatch metaphor mixed — square `rounded-sm` swatch in `ScenarioChartTooltip` + `ChartLegendBar` vs round `rounded-full` dot in the Compare tooltip + legend. Pick one (recommend the dot). (/luis 2026-06-24)
- [x] [S] Gridlines solid at full opacity — `theme.ts` grid is `strokeDasharray:'0'`, `strokeOpacity:1`, against the "few/quiet gridlines" lens. Fix: dashed + ~0.5 opacity. `theme.ts` sharedChart.grid (/luis 2026-06-24)
- [x] [S] (no change needed — already covered) Axis-strip labels are `text-[10px]` with no mobile bump — below the ≤639px legibility floor. Fix: responsive size bump. `ChartAxisStrips.tsx` (/luis 2026-06-24)
- [x] [M] (lens — design decision, not pure drift) Chart card has no hero number — the flagship surface's largest figure is the 12px end-of-line label (`renderEndDot`). Consider a quiet hero above the chart. `page.tsx` chart card (~:2193) (/luis 2026-06-24)
- [x] Stage 1 — `event_scenario_membership` table + 1:1 backfill (`lib/db/sqlite.ts`) — DONE 2026-06-17. Junction table (PK eventId+scenarioId, enabled/isDraft/overrideJson) + reverse index, inline in sqlite.ts; run-once transactional 1:1 backfill → ships dark, projections byte-identical. (/luis 2026-06-17)
- [x] Stage 2 — `lib/utils/scenarioMembership.ts` `resolveScenarioEvents` + composite `mergeOverride` + unit tests — DONE 2026-06-17. Pure resolver (drops absent/disabled, applies override; returns pool refs unchanged when no override → cache-friendly). `mergeOverride` shallow-merges atomics, deep-merges composite wizardInputs. 20 unit tests. (/luis 2026-06-17)
- [x] Stage 3 — API/repo/client/guest — DONE 2026-06-17. New `app/api/event-membership` (GET by scenario/event/all; merge-upsert POST; DELETE). `scenario-events` GET now returns authoring-home ∪ membership superset; POST seeds an enabled cell. `eventMembershipApi`/`eventMembershipRepo` + `guestEventMembershipApi`. SQL smoke-tested. (/luis 2026-06-17)
- [x] Stage 4 — read-path swap + **regression gate** — DONE 2026-06-17. `fetchResolvedEvents(scenarioId)` replaces all 4 base-event load sites + saved-comparison hydration in page.tsx. Regression gate GREEN: full suite **891 tests pass** + `tsc` clean + production build compiles. (/luis 2026-06-17)
- [x] Stage 5 — UI — CLOSED 2026-06-26. The core (Event Matrix modal + swim-lane timeline) was visually verified against the Luis mockup in the 2026-06-19 QA pass. DONE: (1) **Event Matrix modal** (`app/financial-planner/components/EventMatrixModal.tsx` + pure `lib/utils/eventMatrix.ts`) — checkbox grid, rows = events deduped by id grouped Shared/Differs, columns = Base+comparisons, per-scenario override chips. (2) **Swim-lane timeline** (`SwimLaneTimeline.tsx` + pure `lib/utils/swimLane.ts`) — lane per scenario, date-positioned markers, click → opens matrix. 16 helper tests + 4 render-smoke tests. (/luis 2026-06-17)
- [x] Stage 6 — DONE 2026-06-18. (1) **Membership-based save**: `handleRequestSaveScenarios` no longer clones every event — a saved comparison scenario references Base's events via `event_scenario_membership` cells. (2) **Retired the clone-carry path**: removed `handleAddComparisonEventFromLibrary` + the ScenarioManagerBar "From other scenarios" library. (/luis 2026-06-17)
- [x] Scenario Compare — visual QA pass on the Event Matrix modal + swim-lane timeline vs the Luis mockup — DONE 2026-06-19. Matrix modal: header/subtitle, group dividers (Shared/Differs), check/dashed-+ cells, per-column net-worth metric, footer legend all render correctly and match the mockup. Swim-lane: lanes, labels, date-positioned markers, year ticks all render. (/qa 2026-06-19)
- [x] Scenario Compare — Event Matrix override chips show "modified" for composites without a homePrice — DONE 2026-06-19. `summarizeOverride` (lib/utils/eventMatrix.ts) now surfaces a per-type headline value for every composite. 2 unit tests added. Verified jest + tsc. (/qa 2026-06-18)
- [x] 🔴 Scenario Compare — Event Matrix shows every event TWICE when a saved comparison scenario is loaded — DONE 2026-06-19. ROOT CAUSE confirmed: a hydrated saved scenario carries its own event copies with different ids than the live base pool. FIX: `buildEventMatrix` now groups rows by a content `lineageKey` (`tier|type|name|date`) instead of raw id. 3 unit tests added. Verified jest (50) + tsc + LIVE in browser. (/qa 2026-06-19)
- [x] Scenario Compare — swim-lane marker crowding — DONE 2026-06-19. `buildSwimLanes` now positions each event ONCE on the shared union and de-overlaps with MIN_GAP bumped 3.5→6%. Verified live. (/qa 2026-06-19)
- [x] Scenario Compare — swim-lane "ghost" markers — DONE 2026-06-19. `buildSwimLanes` now renders the full event union in every lane: solid where the lane carries the event, a dimmed (opacity .32) + dashed "ghost" where it doesn't, vertically aligned with the lane(s) that do. Clicking a ghost opens the matrix to add it. 3 unit tests added. Verified live. (/qa 2026-06-19)
- [x] Scenario Compare — Event Matrix narrow/mobile overflow — DONE 2026-06-19. `EventMatrixModal.tsx`: name column `w-[48%]`→`w-[40%] min-w-[150px]`, scenario columns gained `min-w-[92px]`, and the column-header label is now `whitespace-nowrap`. (/qa 2026-06-19)
- [x] Stage 1 — restructure header markup into switcher-title + subtitle + right cluster; wire title → `openPopover`. (/luis 2026-06-22) — DONE 2026-06-22. `CurrentPlanDrawer.tsx` header rebuilt: the title is now a single switcher `<button>` (`Current Plan ⌄` / `Archived Plan ⌄`) showing title-line + rotating chevron, with status dot + label + `Effective <date>` as a subtitle beneath. Verified tsc + live.
- [x] Stage 2 — benchmark toggle: reposition the single portaled node (inline-right desktop / full-width strip mobile via flex-wrap+order). (/luis 2026-06-22) — DONE 2026-06-22. Header is now `flex flex-wrap`; the `benchmarkTogglePortalRef` target node moved out of the title block into the right region via CSS only. Verified live.
- [x] Stage 3 — state variants: new/duplicate/review (editable date in subtitle slot, no switcher) + archived / frozen right-cluster buttons in the new layout. (/luis 2026-06-22) — DONE 2026-06-22. All states preserved. Verified tsc.
- [x] Stage 4 — QA pass: dark + warm-ivory, 390px, every header state; confirm popover open/close, version switch, toggle lights body pills, Save/Update/Upgrade fire. (/luis 2026-06-22) — DONE 2026-06-22. tsc clean, no console errors. Live-verified in browser.
- [x] Stage 1 — `FIProjectionChart` component (pure Recharts: base area + base FI ReferenceLine/Dot; optional whatIf overlay), built from ScenarioChart's grammar. (/luis 2026-06-24) — DONE 2026-06-24. New `app/financial-planner/components/FIProjectionChart.tsx`. Pure exported helpers `fiChartGeometry` and `crossoverAnnotation` are unit-tested — `FIProjectionChart.test.ts`, 11 cases. tsc clean + 11/11 green.
- [x] Stage 2 — restructure `FinancialIndependenceDrawer` body to the new layout (hero → chart[base] → reference stats → Coast FI card → Tiers → Settings). (/luis 2026-06-24) — DONE 2026-06-24. Drawer body rebuilt to: date-led hero → `FIProjectionChart` → what-if levers → live tiles → reference stats → Coast FI tinted card → FIRE tiers accordion → Settings. FI figures identical. tsc + fire-calculator/useScenarioNetWorth green + production build green.
- [x] Stage 3 — replace the collapsed What-if with always-on sliders + live result tiles + wire the what-if overlay into the chart. (/luis 2026-06-24) — DONE 2026-06-24. Three always-on sliders (Expenses % / Withdrawal rate / Real growth rate) + "Reset". Live result tiles (FI Date / FI Number / Progress / Age at FI) with per-tile green/red delta. `isLeverMoved` drives the bright what-if target + annotated crossover overlay on the chart. Old accordion-gated What-if + `DeltaRow` removed.
- [x] Stage 4 — states + polish: no-metrics empty state, 390px reflow, dark + warm-ivory pass, a11y (chart summary + labelled sliders), remove dead What-if accordion code. (/luis 2026-06-24) — DONE 2026-06-24. **Regression gate met:** tsc clean, jest 58 green, production build green.
- [x] Stage 1 — Tooltip → number-first **ledger** in `ScenarioChartTooltip`. (/luis 2026-06-26) — DONE 2026-06-26 (commit c8e8996, pushed to master). Number-first ledger shipped: header (year · age) → big total → "▲ this year · +X from today" → per-component rows with own YoY → "This year" events list with icons. tsc + 177 tests + build green.
- [x] Stage 2 — Event markers → **bordered cards** (surface bg + category border/icon, no fill tint) + **one neutral dotted connector per year** up to a single **emerald** on-curve dot; same-year events share one connector/dot. (/luis 2026-06-26) — DONE 2026-06-26 (commit c8e8996). Bordered `EventMarkerCard` for events AND milestones; new pure `getEventCategoryColor`. Per-distinct-year neutral dotted `ReferenceLine` connector → emerald on-curve `ReferenceDot`. tsc + build green.
- [x] Stage 3 — Event **clustering/positioning** → proximity-clustered events **stack as horizontal cards** (front on top, borders of those behind peeking right); **hover fans them out** so each is individually hoverable with its own popover. (/luis 2026-06-26) — DONE 2026-06-26 (commit c8e8996). x-proximity clustering; collapsed = deck (front leftmost, 6px peek); hover fans to 34px centers via CSS transition with each card individually hoverable + popover. tsc + build green.
- [x] Stage 4 — **Hero** → apply the refined hero content/layout (uppercase label → big `--color-accent-display` number → context line + delta badge), **kept right-aligned like today**. (/luis 2026-06-26) — DONE 2026-06-26 (commit c8e8996). Hero refined: uppercase label, 36px `--color-accent-display` number, context line + rounded "+X vs today" delta badge. Income/Outflows anchor to the PEAK year. x-axis **Year/Age/Both** as a shared SegmentedToggle in the chart control bar. tsc + 177 tests + build green.
- [x] Net-worth chart X-axis duplicates the first year label — at 5Y the first two ticks are both "2026". (/qa 2026-06-22) — DONE 2026-06-23. ChartAxisStrips now suppresses any annual-mode per-point label whose calendar year equals the gutter's `firstCalYear`. Verified tsc + production build.
- [x] Timeframe vs impact-horizon year label off by one — the 30Y view's last X-axis tick reads "2055" while the timeline's per-row impact column reads "by 2056". (/qa 2026-06-22) — DONE 2026-06-23. page.tsx now derives `impactHorizonYear` from the chart's actual terminal year (`netWorthData[last].year`) and passes it to both TimelineDrawer instances. Verified tsc + production build.
- [x] Born-milestone age suffix inconsistent — "Keegan Born" carries an age suffix ("· Age -1") while "Baby #2 Born" carries none. (/qa 2026-06-22) — DONE 2026-06-23. BABY_BORN milestones now render a uniform "· Newborn" suffix. Non-born milestone ages use the new `ageInYear` helper and are suppressed when negative. Verified tsc + jest + production build.
- [x] Tax Settings row repeats the identical "<filing status> · <state>" twice in the Current Plan drawer — collapse to one line when both earners share filing status + state, or label each per-person. — DONE 2026-06-24.
- [x] /rent-vs-buy header is missing the theme toggle + user avatar that the main page header carries — DONE 2026-06-26 (already resolved). `LayoutShell`'s fixed top-right cluster already includes `FreePlanBadge`, `ThemePicker`, `HelpMenu`, `NotificationBell`, AND a profile avatar. No change needed.
- [x] Clarify the Current Plan drawer section "% of Net" badges — the percentages sum to well over 100% with an unclear denominator; label the basis (e.g. "% of take-home"). — DONE 2026-06-24. Relabeled "% of Net" → "% of take-home" with a "Share of monthly net take-home pay" tooltip, in both the editor and PlanView.
- [x] Mobile overflow risks (code audit) — the Current Plan plan-history popover is fixed w-[340px] left-0 with no max-w guard; the rent-vs-buy result cards use grid-cols-2 with no responsive prefix. — DONE 2026-06-26. (1) Plan-history popover already guarded by the header redesign. (2) FIXED: rent-vs-buy TCO/opportunity-cost cards changed `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`. Verified tsc.
- [x] Sub-11px text on /financial-planner reads small on desktop — the plan-date pill and the count badges render at 9px. (/qa Jun 2026) — DONE 2026-06-26. Both share `navBadgeClass` (financial-planner/page.tsx:1877); bumped its `text-[9px]`→`text-[10px]`. Verified tsc.
- [x] Plan Timeline "All N" count vs visible rows mismatch — the "All" filter chip counts `allEntries.length` including applied events hidden by default, so the list renders fewer rows than the chip claims. (/qa Jun 2026) — DONE 2026-06-27. `stateCounts` now mirrors the visible list's hide-toggles. Verified tsc + production build.
- [x] Theme consistency: onboarding rendered dark while the in-app Accounts drawer rendered light in the same session (review Obs #3) — AUDIT: the cross-surface divergence is already resolved. FIXED a residual: ThemeContext seeded React state to DEFAULT_THEME and reconciled the persisted preference in a `useEffect`. Moved reconciliation to an isomorphic layout effect. Verified tsc + production build
- [x] Auto-skip the owner step for single-owner households — AccountSetupWizard now derives `singleOwner` (ownerOptions.length === 1) and collapses the owner-column matrix when there's one member with no Joint option. Verified tsc + production build (Obs #9)
- [x] Credit-card editor simple/advanced split — balance, allocation %, utilization, payoff math all show at once; overwhelming as a first impression. Default to a simple view, fold the rest into "advanced". DONE 2026-06-19. Added a remembered Simple/Advanced segmented toggle to CreditCardEditor, default **Simple**, persisted in localStorage. Verified tsc + jest. (Obs #12)
- [x] Reduce account-row icon density — DONE 2026-06-21. Added a self-contained `RowActionMenu` (kebab ⋮ → "Edit account" / "Delete") to `AccountRow.tsx`; replaced the inline ✏️/🗑 buttons in BOTH the mobile and desktop icon rows. Per row: `🔗 ✏️ 🗑` → `🔗 ⋮`. Verified tsc. (Obs #10)
- [x] Simplify the Demographics percentile table on desktop — information-dense and visually competing (Obs #5). DONE 2026-06-22. The income cards carry the decision and the line-by-line breakdown is collapsed behind a "See typical spending & net worth" disclosure (default collapsed). Verified tsc + production build.
- [x] Decide the "Your Numbers" rail on the Demographics step — currently excluded there; confirm intent or surface the FI consequence of the percentile choice (Obs #6) — DECIDED 2026-06-27 (owner): exclusion is INTENTIONAL, no behavior change. Added a clarifying comment at the gate (`app/onboarding/page.tsx`). tsc clean.
- [x] Revisit the Full onboarding path — "Enter full details (~10 min)" reads as a chore; decide whether to keep, simplify, or deprecate it (Obs #7) — DECIDED 2026-06-27 (owner): KEEP as-is, no change.
- [x] Hover tooltip cut off on the right edge — DONE 2026-06-22, confirmed live by owner. The shared `app/components/ui/Tooltip.tsx` positioned its bubble `absolute` + centered inside the trigger. FIX: rewrote Tooltip to portal the bubble to `<body>` with `position: fixed` computed from the trigger's `getBoundingClientRect()` and clamped to the viewport. Verified tsc.
- [x] 🟠 Sign-up dead-end for existing accounts — DONE 2026-06-20, verified live in prod. A returning user who entered a registered email on `/sign-up` got a "that email is taken" error with no path to sign in. FINAL FIX (commit `7f2ae6e`): render our OWN explicit `<Link>` ("Already have an account? Sign in" / inverse) below the Clerk component on both auth pages. Verified tsc + deployed + confirmed both links present in prod SSR HTML.
- [x] Credit card UI broken/poor on mobile; replace with "Add credit card" flow similar to adding holdings, then prompt for details
- [x] Fix input boxes for fixed and discretionary expenses on mobile — PlanRow's mobile Row 2 is a flex row, but the expense/investment item-wrapper was a plain `flex flex-col` that collapsed to content width, shrinking the `flex-1` amount input. Made both wrappers `flex-1 min-w-0`.
- [x] Fund Strategy surplus routing can exceed a tax-status cap when a later account-group bucket also matches the capped account — added a claim mechanism to routeSurplus mirroring withdrawByPriority. All 166 allocation/projection/irs-limit tests pass.
- [x] Adding holding/ticker on mobile: move search bar to bottom with matched results displayed above it — SecuritySearch now elevates into a mobile bottom sheet (results scroll above, search input + Done pinned at bottom, focus moves to it on open). Desktop dropdown unchanged.
- [x] Holdings overflow horizontally on mobile when values have too many digits — the metrics block was a single nowrap shrink-0 flex row. Made it `flex-wrap` (right-aligned) on mobile, `sm:flex-nowrap` on desktop.
- [x] Font, icons, and UI elements too small on mobile — RESOLVED (Jun 2026): global mobile legibility floor — a single max-width:639px block in globals.css raises the smallest text utilities across all ~950 occurrences; ICON_BTN_BASE touch target → 36px on mobile; AccountsBalanceSheet balance column 70→80px. Verified tsc + production build.
- [x] Plan investment/savings categories should auto-link after being added in the previous onboarding step — handleAddInvestmentCategory now uses the shared suggestAccountLinks heuristic (type/owner/tax/name) instead of an exact string match.
- [x] Accounts drawer "Updated -1 days ago" — balanceFreshnessLabel/getBalanceFreshness now compute the day diff on a shared UTC calendar-day basis and clamp future-dated saves to 0. Handles full ISO timestamps too. 6 unit tests added
- [x] /rent-vs-buy: fixed PlanyFI wordmark overlaps banner/content — LayoutShell now reserves top space (pt-16) on pages that render the fixed wordmark/bell (showBell). Verified: tsc + production build
- [x] Guest "Saved for this session" toast stacks twice after onboarding skip — useUpgradeNudge now rate-limits the nudge by TIME (first save, then at most once per 30s) instead of by save count.
- [x] Tune guest beforeunload warning — the warning no longer treats onboardingCompleted as data; it now requires real content (accounts, or a budget with line items / income). Verified: tsc + production build
- [x] 🔴 Compare-mode delete destroys REAL accounts — gated both handleDeleteEvent and handleDeleteWithAccounts on the existing skipAccountSideEffects flag (TimelineDrawer). In Compare mode (in-memory eventsStore) deleting a cloned composite now just drops the event from the scenario. Verified: tsc + production build
- [x] 🔴 ADDED_BY_EVENT guard blocks ALL cross-event category-only removes — replaced the dead atom-id carve-out with serializable composite-level ownership. expandEvents() now stamps each atom with sourceEventId/sourceEventName. 252 financial/scenario tests pass
- [x] 🔴 ADDED_BY_EVENT is a Symbol and JSON-serialization strips it — same fix: ownership is now the serializable sourceEventId line-item field instead of a Symbol. Added a test proving a chained apply (home A's mortgage added, JSON round-trip, home B's category-only remove) preserves home A's line
- [x] 🔴 Comparison save flow is non-atomic delete-then-reinsert — reordered handleRequestSaveScenarios to insert the new generation FIRST (with rollback of partial inserts on error), then delete the old isComparison rows only after every new row + its events are durably written. Verified: tsc + production build
- [x] Base-scenario fallback can promote a comparison row to base — fixed to `find(s => !s.isComparison) ?? allScenarios[0]`. Verified: tsc + production build
- [x] expandEvents expense disambiguation — two fixes: (1) disambiguation now runs in DATE order; (2) EXPENSE_CHANGE update/remove now match by linkedAccountId first when the event targets an account a line is bound to. Tests added for both.
- [x] Custom add-account impacts silently dropped in Compare mode — added pure `syntheticAccountsFromImpacts(event)` (syntheticAccountStubs.ts). Wired into scenarioProjectionCore §10b (engine now projects the inheritance/windfall/loan balance) AND withSyntheticAccountStubs. 5 unit tests added
- [x] Compare + monthly resolution mis-decodes the selected year — toggling Compare now resets resolution to 'annual' and selectedYear to null (handleCompareModeChange in page.tsx). Verified: tsc + production build
- [x] ComparisonTable renders all "—" after clicking the "Current" bar — two-part fix: (1) toggling Compare resets selectedYear; (2) ComparisonTable now clamps displayYear into the projected non-current [minYear, maxYear] range. Verified: tsc + production build
- [x] 🔴 Compare: scenario income events barely affect net worth — ROOT CAUSE: not Compare-specific. routeSurplus deposited extra surplus into IRS-limited accounts, then the engine's post-hoc IRS clawback deleted it WITHOUT re-routing it anywhere. FIX: routeSurplus now takes per-limit-group deposit rooms and honors IRS limits at routing time; surplus no bucket can absorb falls back into cash accounts (`__cash-overflow__` pseudo-bucket). 847 tests pass.
- [x] Onboarding savings slider doesn't match the resulting plan — handleComplete now passes `monthlySavingsTarget` (the exact $/mo the Expenses step displayed, take-home × rate); buildBudget scales the benchmark 401k/Roth/emergency mix to that total.
- [x] FI year inconsistency — rail's calculateOnboardingFI was compounding only the slider's savings $ while the planner engine accumulates the FULL surplus (take-home − expenses) into accounts; YourNumbersRail.useFI now uses (monthlyNet − monthlyExpenses)×12 as annualSavings.
- [x] Planner first-run tour — (1) overlap: above-placement used a hardcoded 140px height estimate; popover now anchors its bottom edge via translateY(-100%); (2) outside click no longer ends the tour (backdrop inert; Skip / Got it / Escape are the only exits — Escape handler added)
- [x] Investment and savings contributions still not auto-linking in detailed onboarding — ROOT CAUSE: the PlanEditor wizard-prefill effect read + `removeItem`'d sessionStorage `planWizardData` on its first run while `accounts` was still loading async (empty `[]`). Added an `accountsLoaded` flag gated on it so the payload is consumed only after accounts resolve. Verified tsc + production build
- [x] When multiple household members have the same account type, only one person's accounts are carried into the plan — SAME root cause as the auto-link fix above. With the `accountsLoaded` gate the prefill now maps every account to its own line item. Verified tsc + production build
- [x] Credit Card and Auto Loan accounts show "N/A" for tax status — replace with blank or a simple label like "Debt" — DONE 2026-06-22. `typeMeta` now returns `tax: isLiability ? 'Debt' : d.taxStatus`. Verified tsc.
- [x] No input box shown when fixed/discretionary expenses are collapsed — DONE 2026-06-22. DECISION (owner): make the single-total input editable & authoritative, items preserved. FIX: removed the read-only branch so aggregate mode always renders the editable lump-sum input; `totalMonthlyExpenses` excludes aggregate-type items from the itemized sum and adds the lump-sum. Verified tsc + jest (179).
- [x] Financial Planner options overflowing on mobile with Compare button present — DONE 2026-06-22. FIX (ChartControlsBar.tsx): left tabs container → `min-w-0 flex-1 overflow-x-auto scrollbar-none`; right lens group → `shrink-0 pl-2`. Verified tsc.
- [x] 🟠 FI status truncated by chart timeframe — selecting a shorter projection window (e.g. 5Y) flips the Financial Independence panel to "FI not yet reached" even when the plan reaches FI beyond that window. ROOT CAUSE: `calculateFireMetrics` scans the `projectionPoints` it's handed for the first year netWorth ≥ FI number, and the caller feeds it the timeframe-truncated series. FIX: page.tsx adds a second `useScenarioNetWorth` at `fiHorizonYears = Math.max(lifeExpectancyYears, 30)` — independent of `timeframe`. Zooming to 5Y/10Y no longer flips FI to "not reached". (/qa 2026-06-22) — DONE 2026-06-23. Verified tsc + jest (fire-calculator 15, age-calculator 12, +239 core projection/allocation tests) + production build.
- [x] Timeline milestone ages off by one / "Age -1" — the Plan Timeline shows "Keegan Born · Feb-2026 · Keegan · Age -1". ROOT CAUSE: TimelineDrawer.tsx:1236 `calculateAge(member.birthDate, new Date(event.date))` — `new Date('yyyy-mm-dd')` parses as UTC midnight (prior day in US timezones). FIX: TimelineDrawer now uses the new timezone-safe `ageInYear(birthDate, eventYear)` and BABY_BORN milestones render "· Newborn". (/qa 2026-06-22) — DONE 2026-06-23. Verified tsc + jest + production build.
- [x] Age conventions disagree across views — for the same calendar year the net-worth chart's age axis and the Add-Event date picker show different ages. (/qa 2026-06-22) — DONE 2026-06-23. Standardized on ONE calendar-year convention (`calYear − birthYear`, timezone-safe) across the chart age axis/tooltip, the Plan Timeline, and the Add-Event date-picker preview. New shared helpers `ageInYear` / `birthYearFromString` (age-calculator.ts, 12 tests). Verified tsc + jest + production build.
- [x] Accounts drawer price refresh has no feedback or error handling — DONE 2026-06-24 (commit 43badbf). `refreshAllPrices` now try/catches, exposes an `isRefreshingPrices` state (spinner + disabled on BOTH refresh triggers), and returns a typed `RefreshPricesResult` so the caller toasts the outcome. Verified tsc + production build. (/qa Jun 2026)
- [x] 🟠 Plan "Remaining" disagrees across surfaces (verify) — DONE 2026-06-24 (commit 43badbf) — VERIFIED + made legible (owner chose legible over reconcile). ROOT CAUSE: two fully independent code paths. Not one "wrong" number — genuinely different metrics. FIX: added an info tooltip to each "Remaining" label explaining the basis. Both formulas left unchanged. Verified tsc + production build. (/qa Jun 2026)
- [x] Silent save-failure handling — failed DB writes are swallowed with no toast. (/qa Jun 2026) — DONE 2026-06-26. All five surfaced: (1) TransactionEditModal gained `useToast` + catch blocks. (2) AccountsContent handleUpdateBudgetLink wrapped in try/catch + toast.error. (3) AccountSetupWizard opening-balance save now toasts "Accounts were added, but their opening balances couldn't be saved…". (4) TransactionsDrawer loadTransactions now throws on non-ok. (5) TimelineDrawer handleDismissSuggested now toasts on failure. Verified tsc + 490 tests green.
- [x] Left-edge event cluster fans out under the "Events" gutter label — hovering the leftmost chart cluster (e.g. 2026) expands its cards leftward past the chart grid, overlapping/truncating the "Events" axis label. (/qa Jun 2026) — DONE 2026-06-27. ChartAxisStrips now computes the deck's raw left edge and adds a `clampPx` correction to the wrapper's `translateX`, so any edge cluster's cards stay inside [0, dataWidth]. Verified tsc + production build.
