# Planyfi — App

## Engineering

### Free tier / monetization (prerequisite for public beta)
Business model (PIVOTED 2026-06-15): Account REQUIRED to use the app (guest flow retired/dormant). Free accounts get full feature access AND data persistence to the DB. Premium ($5/mo or $50/yr) unlocks scenario comparison, unlimited saved scenarios, CSV export, priority support. Persistence stays a Premium concept, to be limited deliberately (retention window) — see below.
- [x] Require account creation to use the app — DONE 2026-06-15. `middleware.ts` now protects all page routes except `/sign-in`/`/sign-up` (+ public API routes); unauthenticated → `/sign-up`. `ProfileContext` flips `isSessionOnly`/`setSessionOnly` to key off `isGuest` (not `!isPremium`), so authed free users persist to the DB. Removed the "Continue without an account" sign-in/up bypass + the onboarding "Previous session cleared" / guest CTAs; `GuestSessionBanner` now early-returns for authed users. Guest store + api-client guest router kept dormant for the future demographics/comparison mode. Updated CLAUDE.md + Help FAQ + deployment-strategy memory. Verified tsc + production build; in-browser confirmed signed-out `/financial-planner` and `/onboarding` redirect to `/sign-up`. (Signed-in free-persistence + new-user onboarding-persists need an authed click-through.)
- [x] Free-tier persistence limit — DECIDED + BUILT (Phases 0-2), 2026-06-15, branch `scenario-comparison` (unmerged). Mechanism: **30-day read-only freeze** (never deletion), anchored to the user's first save. Free plans persist normally, then become view-only once the window elapses; upgrading lifts it instantly; nothing is ever deleted. **Phase 0** (data model): `profiles.firstSavedAt` (anchor, stamped on first budget/account/onboarding write) + `profiles.plan` (Clerk plan mirror); migration backfills `firstSavedAt = deploy time` for existing users with data so nobody freezes day one; `FREE_RETENTION_DAYS=30` in lib/constants.ts. **Phase 1** (server, authoritative): `lib/api/retention.ts` `assertNotFrozen` → 403 `retention_frozen` on 35 mutating handlers across 14 routes (reads stay open; profiles/onboarding/notifications/projected-balances excluded); plan mirror synced via the Clerk billing webhook + admin/set-plan, lazily backfilled from Clerk. **Phase 2** (client): `ProfileContext.retention`, `RetentionBanner` (dismissible countdown in last 14d + non-dismissible frozen "view-only/Upgrade"), global `fetchApi` 403 → upgrade modal + toast, `CurrentPlanDrawer` read-only ("View Only" badge + "Upgrade to edit"). Verified in browser both paths (non-frozen: loads + guarded `PUT /api/budgets 200`; frozen: banner + locked editor + 403 + modal/toast). Phase 3 + rollout follow-ups below.
- [x] Free-tier retention freeze — Phase 3 (notice + export) — DONE 2026-06-17. The two substantive parts (in-app notifications + free JSON export) and the read-only drawer polish all shipped (details below). The two remaining follow-ups (optional email reminders + the manual Clerk rollout) are split into their own items directly below. Deferred from the Phases 0-2 build above. (1) [x] **In-app notifications** at T-14 / T-7 / T-1 and on freeze — DONE 2026-06-17: `/api/notifications/generate` now reuses `getRetentionState` and emits one notification per milestone (2 weeks / 1 week / final day / frozen), deduped by a stable `referenceId` (`retention_14/7/1/frozen`, regardless of read/dismissed so no re-nagging); clears all `referenceType='retention'` notices on upgrade. New `retention_freeze` NotificationType/`retention` referenceType; NotificationBell's action fires the upgrade modal (`show-upgrade-modal`) instead of a route. Messages also point users to the free "Download my data" export. Verified tsc + production build. (2) [x] **Free "Download my data" (JSON) export** button — DONE 2026-06-17: `GET /api/user-data` (paired with the existing DELETE wipe) exports the user's whole dataset (profile + all per-user tables, child tables scoped via account/scenario subqueries, defensive `safeAll`) as a downloadable JSON; read-only so it stays open even when frozen. ProfileDrawer "Download my data" button (always available, emphasized accent style in the last 14 days / after freeze). (3) **Email reminders** at T-7 + on freeze — needs a transactional email provider (only R2 backups wired today; e.g. Resend); optional. (4) [x] **Per-drawer read-only polish** — DONE 2026-06-17: shared `app/components/RetentionLock.tsx` (`useRetentionLock` + `RetentionLockBadge`/`RetentionLockBanner` + `openUpgradeModal`); applied "View Only" badge + frozen banner + disabled (pointer-events-none) editor body to AccountsDrawer (hides add/link/refresh + save, shows Upgrade-to-edit), AllocationStrategyDrawer/Fund Strategy (mutations routed through a frozen-guarded `emitChange`, Reset hidden), and TimelineDrawer (base timeline only — `!eventsStore`, so in-memory Compare scenarios stay editable; footer Add hidden). Mirrors the CurrentPlanDrawer lock. Verified tsc + production build. **Rollout (do before shipping):** set prod account(s) to `premium` in Clerk so the 30-day clock doesn't freeze the owner (prod uses pk_live, separate from the dev pk_test instance used for testing). Possible later phase: hard-delete of long-abandoned *frozen* accounts (with warnings) for storage hygiene — explicitly out of v1 to keep the "never lose data" promise. **REMAINING (split into their own items below):** email reminders (optional) + the prod-owner premium rollout.
- [ ] 🟠 Retention rollout (before shipping the freeze) — the owner must become premium so the 30-day clock never freezes them. DECISION 2026-06-19: instead of a manual Clerk premium flip, the owner will go through the REAL Clerk billing upgrade flow in prod (deliberately testing that path end-to-end); upgrading lifts the freeze instantly. The owner has a 30-day window from first save (`firstSavedAt` backfills to deploy time) to do this before freezing. prod uses pk_live, separate from the dev pk_test instance. Still TODO: any OTHER prod accounts that should never freeze (admin/demo) need the same treatment.
- [ ] Retention email reminders (optional) — transactional email at T-7 + on freeze, complementing the in-app notifications already shipped. Needs an email provider (only R2 backups are wired today; e.g. Resend): provider account + API key, a send helper, and a trigger (cron over `getRetentionState`, or piggyback the notification-generate pass). Lower priority than the in-app notices since those + the banner already cover the freeze UX. Split out of the Phase 3 item above.
- [x] Guest-flow dead-code cleanup — DONE 2026-06-17 (DECISION: keep dormant for the future no-account mode, annotate + isolate; no deletion). (1) Added clear "DORMANT" file/section headers correcting the stale pre-pivot model wording on the guest scaffolding: `lib/db/guest-store.ts`, the `lib/db/api-client.ts` data-layer router, `GuestSessionBanner.tsx`, and `/api/guest-migrate`. (2) Replaced GuestSessionBanner's blocking `confirm()` "Start over" (froze the renderer during verification) with a non-blocking inline confirm (Clear / Cancel). (3) **Live bug fixed:** ProfileDrawer's Sign Out showed authenticated *free* users a "your data will be permanently deleted" warning and `handleFreeSignOut` actually `DELETE`d their persisted data on sign-out — guest-era behavior that contradicts the post-pivot model (free authed users persist). Sign Out now just signs out for all authenticated users; removed the warning UI + `handleFreeSignOut` + `showSignOutWarning`/`isClearing` state. Remaining inert branches (UpgradePromptModal guest variant, useUpgradeNudge session toast + Ctrl+S, checkout snapshot) left dormant as decided. Verified tsc + production build.
### Infrastructure / ops (before inviting friends)
- [~] ~~Stripe account setup~~ — REPLACED with Clerk Billing (migrated May 2026)
### Deferred / reconsidered
- [~] ~~Provisioning layer — automate per-user Railway instance creation via API~~ — DROPPED. Shared multi-tenant SQLite + Clerk userId scoping already covers isolation. Per-instance economics kill $5/mo margin. Revisit only as Enterprise tier.
- [~] ~~Custom domain routing — map user.planyfi.app to each user's Railway instance~~ — DROPPED with the above.
- [~] ~~Data export — let users download their SQLite file~~ — REPLACED with per-user JSON export (SQLite file would leak other users' data in shared-DB model).
### QA sweep (Jun 2026) — full sweep
- [x] a11y: add aria-labels to icon-only buttons — added `aria-label`/`title` to the shared Drawer.tsx close X and every custom-header drawer close button (CurrentPlan, Accounts, Transactions, ProjectedPlan, Timeline ×2, AllocationStrategy); AccountSetupWizard close X + duplicate/remove buttons now carry descriptive aria-labels (e.g. "Remove Checking"). Verified tsc + production build (/qa Jun 2026)
- [x] Surface silent save states: AccountsContent bulk save has no in-flight indicator; onboarding `void updateProfile(...)` swallows errors; NotificationBell shows "No notifications" during initial fetch — add loading state (/qa Jun 2026)
### Code review (Jun 2026) — scenario-comparison branch cleanups
Source: /code-review high on the unmerged `scenario-comparison` branch (worktree included). Bugs from the same review are in Bugs / Issues, tagged (/code-review Jun 2026).
- [x] ScenarioManagerBar name input commits onUpdate per keystroke → new comparisonScenarios array → full recharts LineChart + ComparisonTable rebuild on every character (ScenarioManagerBar.tsx:127; projection engine is cache-shielded but all downstream UI work repeats). Use local draft state committed on blur/Enter (pattern: ControlledNumberInput)
- [x] useComparisonProjections ref-cache pins full ScenarioProjectionResult incl. monthlyData (~360 points × per-account record maps per scenario) that no Compare consumer reads — strip monthlyData before cache.entries.set (useComparisonProjections.ts:139)
- [x] ComparisonChart duplicates ScenarioChart code verbatim: nice-domain/tick math (ComparisonChart.tsx:87–96 = ScenarioChart.tsx:232–241, repeated again at :258–267) and the click-to-select-year mousedown effect (`.recharts-cartesian-grid` query — 3rd dependence counting ChartAxisStrips.tsx:48). Extract shared niceDomain() helper + useChartYearClick() hook
- [x] AccountSetupWizard hardcodes its own account-type taxonomy — now derives GROUPS from ACCOUNT_TYPES/TYPE_DEFAULTS via a UI_GROUPS map (4 UI buckets, the two debt account-groups collapse into one "Debt"; 'Custom' excluded as it needs the full form), so any new account type auto-surfaces. Replaced the inline Unicode-minus `money()` formatter with shared `fmt` from lib/utils/format.ts. Verified tsc + production build
- [x] Build "Luis" Claude skill/agent as in-app design consultant for Planyfi — interviews Mitch on design preferences and Planyfi design system; outputs 3 HTML options per request to a designated folder

## Product
- [ ] Add help/info icons to each drawer/page with in-depth explanations; explore on-screen tutorial as well
- [ ] Add calculator section: rent vs buy, buy a home, estimated taxes — pull from current plan inputs with ability to adjust and see quick impact
- [ ] Rent vs. Buy — compare two homes by address, or rent vs. buy a single property
- [ ] Home Purchase — zip code lookup for median home price, county-level property tax, flood/wind insurance zones
- [ ] Rent vs. Buy — pull property data by address (Zillow/Redfin API or manual entry)
- [ ] Rent vs. Buy — surface as linked tool inside Scenario Planner HOME_PURCHASE event and Plan
- [>] Integrate US census data more deeply into app
- [>] Add view mode for filtering/browsing different demographics
- [ ] 🟠 Send beta version to Zach, Julie, AJ, James, and others. Original blockers all CLEARED: free-tier limit (retention freeze — built), backups (R2 daily cron — confirmed working), error monitoring (Sentry + PostHog — wired in code). REMAINING before invite: (1) **Deploy master** — `scenario-comparison` was MERGED into master 2026-06-19 (retention freeze + Scenario Compare + Allocation view & Primary-cash-flow account hidden behind flags for beta); the merge is local/unpushed + undeployed. tsc clean, 924/924 tests, build green. (2) Verify `NEXT_PUBLIC_SENTRY_DSN` + `NEXT_PUBLIC_POSTHOG_KEY` are actually set in Railway prod. (3) Owner self-tests the real Clerk upgrade flow in prod (doubles as the retention rollout — see above). (4) Decide whether beta testers get comped premium or accept the 30-day free-tier freeze window (a beta running >30 days will freeze their data otherwise).
- [ ] Add recurring transactions to accounts/holdings — auto-approximate periodic additions (weekly/monthly) without manual entry; show Estimated Balance vs Latest Balance (consistent with checking/savings approach)
- [>] Scenario Comparison feature: ability to hide/adjust Current Plan, Events, Milestones, Market Assumptions; full plan summary/net worth for a given year; line chart plotting net worth, income, expenses for up to 3 scenarios
- [ ] Add button that generates a generic LLM prompt — instructs any AI tool to review attached transactions and categorize them against the user's current plan (specific categories + sub-categories) in a format uploadable to Planyfi for plan comparison or updates
- [x] Review Fund Strategy and Roth Conversion Ladder — verify accuracy, defaults, and quality of explanations. /qa Jun 2026: fixed Roth target-bracket text hardcoding MFJ figures (now uses plan's filing status), fixed deficit-withdraw "Available" totals double-counting overlapping accounts, updated drawer subtitle to mention Roth conversions. Verdict: Roth tab belongs in Fund Strategy (fund-movement logic); keep Surplus/Deficit as separate tabs. Residual items below.
- [x] Research available US datasets for household income, saving/investing, and spending — delivered themed HTML report at design-mockups/us-data-report.html
- [x] Add insights to Current Plan page: hover/tap to see typical US household spend by category with user comparison; show impact of reducing expenses on net worth — comparison badges (CategoryBenchmarkBadge + DemographicBenchmarkContext) were already shipped and auto-derive the demographic from the primary member's birthDate (onboarding persists it), so they appear with no manual selection. NEW: completed the scaffolded net-worth impact (Deliverable B3) — `lib/hooks/useBenchmarkImpact.ts` runs the pure projection core once per over-benchmark expense category (reduced to the typical level via the existing `applyBenchmarkToPlan`), diffs terminal net worth vs. the already-computed baseline (netWorthData), and publishes a per-category map. Mounted via `BenchmarkImpactProvider` threaded financial-planner/page.tsx → CurrentPlanDrawer → PlanEditor; gated on the drawer being open and memoized so the extra projections never run on the main view. Badge now shows "Reducing to the typical level could add about $X to your long-term net worth." Test added (4 cases: over→positive delta, under→omitted, disabled/unavailable→null). Verified tsc + jest + production build. (Contributions-impact half split out below — it needs contribution benchmarking first, which was never built.)
- [ ] Current Plan insights — contributions side: add benchmark comparison + net-worth impact for contribution rows (401k/Roth/emergency). The expense side is done; the demographic data already carries contribution benchmarks, but there's no contribution badge (B2) yet, so "increasing contributions could add $X" needs the badge + an increase-to-benchmark variant of the impact simulator
- [ ] Reframe Current Plan as standalone input feature; Financial Planner shows the output/results — make the distinction clearer in UX
- [ ] Add multiple lines to net worth chart based on different market performance assumptions (e.g. 6% vs 7%)
- [ ] Fund Strategy — auto-mirror Deficit Strategy default from Surplus Routing order (drain accounts in reverse of fill order) and expose manual deficit config as "advanced", so most users never hand-configure the deficit waterfall. Note: deficit's "reduce contributions" step has no surplus analog, so the two tabs stay distinct.
- [x] Consult Claude on improving account-adding UX; review full onboarding flow in separate project chat — review delivered (design-mockups/account-onboarding-ux-review/, 12 observations). Roughly half already shipped: inline balance entry in AccountSetupWizard, "{n} to fill later" progress nudges, colored holdings reconciliation delta, auto-link to budget, the onboarding "Your Numbers" rail. Outstanding observations triaged into discrete items under Engineering (New User Experience) below.
- [x] Show warning popup if non-premium user tries to refresh page — added a beforeunload handler in ProfileContext for session-only (non-premium) users that fires the native leave-site prompt only when the guest store has data (checked at unload time, so fresh users aren't nagged). Verified in-browser via synthetic beforeunload dispatch (defaultPrevented=true with data present)
- [x] Empty-state CTAs: planner "No projection data" panel and Allocation tab "Add holdings" message are dead ends — add buttons opening the Current Plan / Accounts / holdings flows — ScenarioChart empty state now offers "Set up your plan" + "Add accounts" buttons (open the Current Plan / Accounts drawers); AllocationChart's "Add holdings" empty state now has an "Add holdings" button opening the Accounts drawer. Wired via optional onAddPlan/onAddAccounts props from financial-planner/page.tsx. Verified tsc + production build (/qa Jun 2026)
- [x] Compare mode polish: identical scenario line exactly hides the Base line (consider dash/offset for Base); first added scenario is named "Scenario 2" (prev.length+2 in financial-planner/page.tsx — confirm intent or fix); ComparisonTable lacks an empty state when no scenarios are selected and shows bare "—" for out-of-range years; scenario chip calendar badge counts events only while the drawer also lists milestones (/qa Jun 2026)
- [x] Accounts drawer net-worth mini panel: asset-class sub-row labels truncate to invisible and the liabilities amount clips at the panel edge — root cause was the drawer's summary sidebar pinned to `w-48` (~168px content) at the 1200–1450px breakpoint, leaving sub-row labels only ~28px after the fixed %/balance columns. Widened the sidebar to `w-60` (AccountsDrawer.tsx) so labels get ~76px. Also bumped ScenarioSummaryBox's `AMT_COL` 78→88px (matching ComparisonTable's already-88px column, as its "mirror" comment claimed) so negative 7-figure liabilities no longer clip. Verified tsc + production build (/qa Jun 2026)
- [x] Fund Strategy: "Mo. Expenses" bucket input renders value + "mo" suffix cramped ("6mo" reads as "6no") — fixed in shared ControlledNumberInput: the suffixed input had a fixed `paddingRight: 28px` (sized for the 1-char "%") so the value butted against multi-char suffixes. Now scales as `suffix.length * 7 + 21`px ("%" stays exactly 28px → no regression; "mo"→35px, "/yr"→42px). Benefits every suffixed input app-wide. Verified tsc + production build (/qa Jun 2026)
- [x] Add explanatory text at top of each onboarding page where missing — audited all Quick + Full path steps. Added the genuinely-missing "Your Accounts" heading + explanation to the **full-path Accounts step** (OnboardingAccountsWrapper had no header at all; gated on the AccountSetupWizard being closed, matching the existing Next/Back footer). Rewrote the **Household & Income** subtitle from the tautological "Tell us about your household and income" to explain the why ("We use this to estimate your taxes and take-home pay — the basis for every projection that follows"). Broadened the **Expenses & Savings** subtitle to actually cover savings (page is "Expenses & Savings" but copy only mentioned expenses → now "…how much you set aside to save. We'll project your plan from what's left over…"). Welcome / Demographics / quick-path Accounts already had adequate intros; full-path plan editor keeps its existing "Create Plan / Set up your financial plan" header. Verified tsc + production build.
- [x] On accounts page (post-account-add in detailed onboarding): add explanation that user can add underlying holdings; alternatively consider skipping this page given new onboarding account-add flow — DECISION: skip the page entirely. The post-wizard "Your Accounts" review list added a step without enough value (the self-contained AccountSetupWizard already captures types, balances, owner, and auto-links to the budget; holdings stay an in-app task disclosed by the wizard's HOLDINGS_NOTE). OnboardingAccountsWrapper rewritten to render AccountSetupWizard inline (embedded/isOnboarding); onComplete → goToStep('fullPlan'). Added onboarding-gated wizard props: `onBack` (renders a Back button → household, since embedded mode hides the close X) and `existingAccountCount` (returning from the plan editor with an empty cart shows a "Continue" primary instead of forcing another account). In-app Accounts-drawer usage untouched (both props omitted → identical overlay + close X). Verified tsc + production build
- [x] Add default sub-categories when users expand a category — e.g. expanding Transportation shows Car Payment, Auto Insurance, Gas instead of just repeating the parent category — DONE 2026-06-19. Two changes in `app/plan/edit/page.tsx`: (1) Expanded `SUBCATEGORY_SUGGESTIONS` to cover every `DEFAULT_EXPENSE_CATEGORIES` name (added Insurance/Childcare/Restaurants/Shopping/Vacation/Charitable Giving; renamed Transportation's "Insurance"→"Auto Insurance" per the example; concise ≤5-item lists). (2) Rewrote `expandIntoSubCategories`: when a flat category has presets and no existing subs, it now SEEDS the suggested sub-category rows ($0 each) — the user sees a real itemization menu (Car Payment, Gas, Auto Insurance, Maintenance, Parking) instead of one row repeating "Transportation". Any existing spend is carried (split-aware) into a neutral **"Other"** bucket so the plan total never silently changes (omitted when the category was empty). Categories with no presets keep the old parent-name fallback; DB-loaded subs still just reveal visually. Readonly PlanView already filters $0 subs so no clutter there. Verified tsc + production build.
- [x] Require account creation to use the app — DONE 2026-06-15 (see the implemented entry under Engineering → "Free tier / monetization"): account now required, free tier persists to the DB, guest flow retired/dormant. Follow-ups (free retention limit, guest dead-code cleanup) tracked there.
- [ ] Remove Split option for investment and savings — use separate contribution entries per account instead; same name allowed (e.g. Roth IRA) with owner tag to differentiate
- [ ] If auto-showing Credit Card in debt section, add info icon or asterisk clarifying it only applies if carrying a balance beyond the statement balance (i.e. not already covered in normal expenses)
- [ ] Add similar info icon/asterisk to Auto Loan in debt section — note it's typically already captured under Transportation as a car payment
- [ ] Revisit Balance History tab — clarify purpose, explore improvements, consider relocating
- [ ] Allow users to rename accounts before finalizing the add
- [ ] Add persistent banner or indicator showing user is on free account with upgrade CTA — visible outside of profile section
- [ ] Revisit and update UI for FI section

### QA Sweep (Jun 2026) — component consistency + light contrast
Source: /qa sweep focused on component consistency (text, icons, dropdowns, popups) and light-theme contrast. Fixed in-sweep: muted/tertiary text + chart-axis contrast (now WCAG AA), Fund Strategy tabs converted to underline style, Timeline legend "Milestone"→"Milestones". Remaining (deferred):
- [x] Account-group label casing inconsistent across panels — aligned on sentence-case. Hoisted shared `ACCOUNT_GROUP_LABELS` map in AccountsContent.tsx; account-list card headers now use it (were raw title-case `{group}`), matching the balance-sheet summary
- [x] Per-row action icons differ in color — added shared `ROW_ACTION_ICON` / `ROW_ACTION_ICON_DANGER` constants (brand-classes.ts) documenting the convention: resting = subtle gray, neutral hover = muted, destructive hover = red. Applied to CreditCardItem edit/remove. Plan gear's emerald is the intentional *active* (customized) state, left as-is
- [x] Top tab-bar status indicators mix metaphors — formalized convention: count/info = badge, customized/attention = dot. Market Assumptions dot was a hardcoded hex (#34d399); switched to `var(--color-accent)` token so the "customized" signal matches the plan gear's active treatment app-wide
- [x] Plan Summary Waterfall "Expenses" row defaults expanded even with no child rows — ExpRow now detects real children (React.Children); when empty it hides the chevron and renders a non-interactive (non-toggling) row. Benefits Taxes/Investments rows too

Mobile responsive pass (code audit only — viewport emulation wasn't controllable via browser automation; no breakage found, app is consistently responsive). Optional polish:
- [x] Onboarding benchmark table (DemographicsStep) forces horizontal scroll on phones — added a mobile-only (`sm:hidden`) stacked layout: one selectable card per percentile with a 2-col label/value breakdown (radiogroup + keyboard support). Desktop table now gated `hidden sm:block`, no more horizontal scroll on phones
- [x] Credit-card editor Field uses fixed w-40 (160px) input on mobile — made fluid: input wrapper now `flex-1 min-w-0 max-w-[160px]` on mobile (fills the row up to 160px, shrinks on 320px screens), reverts to column width at `sm:`

### Design Review (Apr 2026)
Source: Claude Design review of Planner, Accounts, Current Plan, Events, Milestones, and Net Worth surfaces. Quick wins first: #03, #06, #05, #09 (all S effort). Net Worth order: A → E → B → D → C.

**Planner**
- [ ] Allocation drift panel — show top 3 drifted asset classes (actual vs. target bars) in Net Worth summary rail; rebalance recommendation line [S effort, Med leverage]

**Accounts**

**Drawers (Current Plan / Events / Milestones)**
**Net Worth chart deep dive**
- [ ] Narrative annotations — auto-detect peak, FI crossover, drawdown start; place type-on-chart labels with sentences; max 4 visible, toggleable [S effort, Med leverage]

### QA sweep (Jun 2026) — onboarding + Compare deep dive
Source: /qa full visual sweep as a new Quick Setup user + Compare-mode product review.
- [x] Disclose Quick Setup seeded events — added an `isSuggested` flag on events (lib/types/scenario.ts), set on seeded impact events (INCOME_CHANGE + composites) in buildSuggestedScenarioEvents (retirement/life-expectancy anchors excluded as expected defaults). Persists via guest-store clone + composite payload (atomic survives via spread). Timeline event cards now show an amber "Suggested" pill (tooltip: "Suggested by Quick Setup — not chosen by you") that dismisses on click; editing an event also clears it. Test added. Verified tsc + production build (/qa Jun 2026)
- [x] Onboarding polish: (1) default income percentile is now 50th (was 90th) — both the initial state and the path-reset; (2) benchmark expenses rescale when household composition changes on the Household step. New `impliedFamilySize`/`familyExpenseFactor` helpers (onboarding-benchmarks.ts); an effect ratio-scales all expense categories by the family-size factor when hasPartner/dependentCount change, guarded by an `expensesEdited` ref so it never clobbers manual edits and an `expensesScaledForSize` ref so it's exact/idempotent (Childcare left to its own dependents effect). A test proves the ratio rescale equals re-deriving from getBenchmarks. Verified tsc + jest (254) + production build (/qa Jun 2026)
- [x] Event conflict warning — a later SET-type event silently overrides an earlier one (seeded Career raise 2029 nullified a 2027 "set salary $70K" event); surface a conflict hint in the event form/timeline (/qa Jun 2026). DONE: new pure util `lib/utils/eventConflicts.ts` extracts the SET targets each event writes (INCOME_CHANGE/TAX_CHANGE always; EXPENSE_CHANGE/ACCOUNT_CONTRIBUTION only `action:'update'`; composites expanded via generatedEvents so a JOB_CHANGE salary conflicts with a standalone salary event), groups by target, and flags every earlier event a later same-target SET supersedes (same-date = ambiguous). TimelineDrawer shows an amber "Overridden" pill (tooltip names the overriding event + year) on flagged rows, excluding applied/hidden events. CustomEventForm shows an inline amber warning derived from the draft's generated atomics ("Later, '<X>' (year) resets <field>…" / "This overrides <field> set by '<X>'…"). 16 unit tests added. Verified tsc + jest (90 incl. projectPlan/registry). NOTE: production build couldn't run (another next process held .next/lock); composite event forms (JobChangeForm etc.) get the timeline badge but not the in-form hint — only the Custom form path warns pre-save.
- [x] Align save tiering copy — reframed the guest Save modal (UpgradePromptModal) to the persistence-gated model: body now "Your plan lives in this browser session only. Upgrade to Premium to save it and pick up anywhere." and the CTA reads "Upgrade to Premium" (keeps handleSignUp — a guest must create an account before Clerk checkout, then lands on the free-auth pricing variant). Save-scenarios tooltip already said Premium. Verified tsc (/qa Jun 2026)
- [x] Compare polish batch — all four addressed: (1) chip-row crowding: MarketAssumptionsPopover gained a `compact` icon-only mode (corner accent dot for custom rates) used inside scenario chips; (2) "+ Add scenario" 3-cap now shows an inline "Max 3 scenarios" pill instead of a title-only disabled button; (3) rail % basis labeled — ScenarioSummaryBox ledger column captioned "% of assets" and the plan-waterfall column "% of income" (the actual bases: totalAssets vs absGross — QA's "% of assets" guess applied to the ledger; the waterfall is income-based); (4) net-worth tooltip debts: ScenarioChartTooltip now groups assets above liabilities with a divider (was a pure magnitude sort), and liabilities draw from a new red LIABILITY_PALETTE so debts never collide with an asset's auto color. Verified tsc + jest (/qa Jun 2026)
- [x] Compare: FI year / retirement age row at the top of ComparisonTable — added a "FI reached" row directly under Net Worth showing the FI year + each non-dependent member's age at FI (e.g. "2041 / age 52/50"), or "Not reached". Computed per scenario via a useMemo that reuses the pure calculateFIRE (real-dollar projection points, per-scenario retirement milestone + market assumptions), mirroring page.tsx's single-scenario fireInputs. Threaded householdMembers/fireWithdrawalRate/fireExpenseDefinition props from page.tsx. Verified tsc + jest (fire-calculator 15, useScenarioNetWorth 32) (/qa Jun 2026)

### Design consistency (Luis)
Source: /luis full review focused on the Financial Planner page + Scenario Compare feature. Design-system/lens drift only (functional bugs go elsewhere). Token discipline is broadly good — no native `<select>`, hex literals are mostly legit chart-palette/SVG. Drift is concentrated below.
- [x] [M] Two segmented-toggle styles for the same control — the Year/Month period toggle in ScenarioSummaryBox (PlanSection) + ComparisonTable is a bespoke `h-6 rounded-md` track with a `rounded-sm` active chip, diverging from the canonical shared `SegmentedToggle` + `themeConfig.controls.toggle` used everywhere else (Yearly/Monthly, timeframe, cash-flow period). Fix: route the summary-box period toggle through the shared toggle classes. `ScenarioSummaryBox.tsx:544` + `ComparisonTable.tsx:696` (/luis 2026-06-17) — DONE 2026-06-17: extracted the canonical SegmentedToggle out of ChartControlsBar into a shared `app/components/ui/SegmentedToggle.tsx` (defaults to `themeConfig.controls.toggle`, callers may override). Routed the ScenarioSummaryBox + ComparisonTable period toggles AND a third verbatim copy found in PlanSummaryWaterfall through it; ChartControlsBar now imports the shared one. Verified tsc.
- [x] [M] Hero number uses the wrong accent role — the big Net Worth figure in ScenarioSummaryBox is `--color-accent`, while the Savings-rate hero and FI hero in the same component correctly use `--color-accent-display` (the deeper `#059669` in light theme; per design-system §4 large hero figures use accent-display). The net-worth headline reads too bright next to its siblings in light. Fix: net-worth hero → `--color-accent-display`; consider same for the compare table's headline Net Worth / Remaining figures. `ScenarioSummaryBox.tsx:168` (secondary: `ComparisonTable.tsx:612,872`) (/luis 2026-06-17) — DONE 2026-06-17: net-worth hero now uses `--color-accent-display` (matching the savings-rate/FI heroes); also routed ComparisonTable's Net Worth + Remaining headline figures' positive branch to accent-display. Negatives keep `--color-negative-fg`; small year labels stay `--color-accent`. Verified tsc.
- [x] [M] Mixed premium-gate metaphor on the same bar — CSV-export gate uses a lock icon + tooltip; the Save-scenarios gate uses a trailing "★" text glyph (`Save ★`). Two metaphors for "Premium" side by side. Fix: standardize on one affordance — recommend the lock icon (matches the existing export gate) or a small "Premium" pill. `ScenarioManagerBar.tsx:243` + `ChartControlsBar.tsx:288` (/luis 2026-06-17) — DONE 2026-06-17: replaced the Save-scenarios gate's trailing "★" glyph with the same lock icon the CSV-export gate uses. One Premium metaphor (the lock) across the bar now. Verified tsc.
- [x] [M] FI panel hardcodes rgba that half-disappears in light theme — the FI section uses `rgba(255,255,255,0.07)` for the progress-bar track + the supporting-row divider, and `rgba(52,211,153,0.22)` for the border. White overlays nearly vanish on warm-ivory, and the dark-emerald border doesn't shift to the light accent, so the panel is noticeably weaker in light mode. Fix: swap to tokens (surface step / `--color-border` / `--color-accent-ring`). `ScenarioSummaryBox.tsx:892,922,945` (/luis 2026-06-17) — DONE 2026-06-17: rgba literals swapped to tokens — panel border → `var(--color-accent-ring)`, progress-bar track + supporting-rows divider → `var(--color-border)`. Reads correctly in light theme now. Verified tsc.
- [x] [S] Two voices for the "nothing to compare" empty state — ComparisonChart shows "Add a scenario to compare projections." at `p-8`; ComparisonTable shows "No scenarios to compare" + subtext at `p-6`. Same state, different copy and padding. Fix: align copy + padding. `ComparisonChart.tsx:95` + `ComparisonTable.tsx:572` (/luis 2026-06-17) — DONE 2026-06-17: both aligned to the same heading ("No scenarios to compare") + subtext ("Add a scenario above to compare projections against your Base plan.") and `p-8` padding. ComparisonChart gained the heading/subtext structure; ComparisonTable bumped p-6→p-8 + trimmed copy. Verified tsc.
- [x] [S] Chip action icons are hand-rolled — the calendar/checklist/remove icons in scenario chips use bespoke `h-6 w-6 rounded-md` instead of the shared icon-button family (`rounded-[7px]`, defined variants in brand-classes.ts). Deliberate compact density, but off-system. Fix: add/use a compact icon-button variant. `ScenarioManagerBar.tsx:154` (/luis 2026-06-17) — DONE 2026-06-17: added compact 24px icon-button variants (`ICON_BTN_GHOST_SM`/`ICON_BTN_ACTIVE_SM`/`ICON_BTN_DANGER_SM`, all `rounded-[7px]`) to brand-classes; applied to the chip checklist + remove buttons, and aligned the calendar/edit button and the MarketAssumptionsPopover compact gear to `rounded-[7px]` so the whole chip row is on-system. Verified tsc.
- [x] [S] ProfileDrawer var() fallback hex is wrong + needless — `var(--color-warning-bg,#2d1f00)` fallback doesn't match the real token (`#2d2008`), and tokens are always defined so the inline fallback is dead weight. Fix: drop the fallbacks, use plain `var(--color-warning-*)` / theme classes. `ProfileDrawer.tsx:354` (/luis 2026-06-17) — DONE 2026-06-17: dropped the dead `var()` fallback hexes on the sign-out warning (bg `#2d1f00`→none, fg `#f59e0b`→none); now plain `var(--color-warning-bg)`/`var(--color-warning-fg)`. Verified tsc.

### Design consistency (Luis) — Fund Strategy (2026-06-22)
Source: /luis review scoped to the Fund Strategy surface (AllocationStrategyDrawer + AllocationSummaryBox + AllocationPriorityList + DeficitPriorityList). Design-system/lens drift only — no functional bugs. Through-line: green is spent as decoration rather than as the accent, and several controls hand-roll classes that `brand-classes.ts` already provides. Token discipline is otherwise good (shared `Select` throughout, no native `<select>`; the Roth enable switch matches its peers across other drawers).
- [ ] [M] Accent overspend / semantic-token misuse — `--color-positive-fg` is used for interactive & decorative green across the surface (every summary-box value, the "+ Add Priority"/"Customize order" links, the Roth mode active segment, the secondary buttons) instead of the accent-role tokens. positive/negative/warning/info are *status* colors; green text/links/active belong to `--color-accent` / `--color-accent-text`. Also a lens violation — every number painted green ⇒ no hero, diluted accent. Fix: interactive/link green → `--color-accent`/`--color-accent-text`; demote decorative number-green to `text-theme-secondary`; keep `positive-fg` only for true positive status (e.g. the "No deficit" line). Light-theme bonus: buttons stop rendering deep `#047857` instead of brand `#10b981`. `AllocationSummaryBox.tsx` 230,250,301,369/377,401 · `AllocationStrategyDrawer.tsx` 339,394 · `AllocationPriorityList.tsx` 356 (/luis 2026-06-22)
- [ ] [S] Fund Strategy shared-class sweep — six small drift fixes that are really one PR: swap hand-rolled classes for the shared `brand-classes.ts` constants + fix the label inconsistency. Pair with the F1 de-green above. Before/after visual (drawer): `design-mockups/luis/fund-strategy-drawer/`. (/luis 2026-06-22)
  - [ ] Roth "Conversion Mode" segmented control reinvents a soft pill toggle instead of the shared `TOGGLE_TRACK_CLASS` + `TOGGLE_ACTIVE_CLASS` / `SegmentedToggle` (also fixes the green-text active state from F1). `AllocationStrategyDrawer.tsx:387-402`
  - [ ] Primary buttons skip `BTN_PRIMARY_CLASS` + the 9px button radius (hand-rolled accent buttons at `rounded-lg`). `AllocationStrategyDrawer.tsx:234,475`
  - [ ] "Edit Target Allocation" / "Fund Strategy" buttons skip `BTN_SECONDARY_CLASS`/`BTN_GHOST_CLASS` (accent-soft + positive-fg text + `rounded-lg`). `AllocationSummaryBox.tsx:399-410`
  - [ ] Hand-rolled inputs where a shared cell class exists — cap input → `INPUT_CLASS_CELL` (its sibling `DeficitPriorityList` already does); drift input → `NUMERIC_CELL_CLASS`. `AllocationPriorityList.tsx:313` · `AllocationSummaryBox.tsx:338`
  - [ ] Three label treatments on one surface — eyebrows → `FIELD_LABEL_CLASS`, in-form labels → `LABEL_CLASS`. `AllocationSummaryBox.tsx` 228,245,260,293 · `AllocationStrategyDrawer.tsx` 386,413,432
  - [~] ~~Terminology drift "Allocation Summary" → "Fund Strategy"~~ — PULLED 2026-06-23 (Luis misread): the "Allocation Summary" rail and the "Fund Strategy" drawer are two distinct surfaces (the rail's "Fund Strategy" button opens the drawer), so a rename would collide. `AllocationSummaryBox.tsx:224` stays.
  - [ ] Micro-badge spec mismatch — unify buy/sell + reduce/withdraw to one compact-badge spec; consider a compact `BADGE_*` variant. `AllocationSummaryBox.tsx:367` · `DeficitPriorityList.tsx:213` · `AllocationStrategyDrawer.tsx:326`

### Scenario Compare redesign — shared event pool (Luis + Claude Design, 2026-06-17)
Design approved. Spec + visual contract: `design-mockups/luis/scenario-compare-workspace/` (`SPEC.md`
= engineering spec, `index.html` = 3-screen mockup). Model: one shared event pool + per-scenario
membership (junction table) + optional overrides + draft-by-default; three views over the pool (Plan
Timeline drawer = author, swim-lane = read, matrix popup = bulk edit). **Contracts:** projection engine
untouched; post-backfill projections must be byte-identical to today. Supersedes the "Applies to
checkboxes" + "Promote scenario to plan" compare items above. Build in stages (see SPEC.md):
- [x] Stage 1 — `event_scenario_membership` table + 1:1 backfill (`lib/db/sqlite.ts`) — DONE 2026-06-17. Junction table (PK eventId+scenarioId, enabled/isDraft/overrideJson) + reverse index, inline in sqlite.ts; run-once transactional 1:1 backfill (one enabled cell per event, anchored to authoring-home scenarioId; createdAt preserved) → ships dark, projections byte-identical. In-memory SQL smoke test verified backfill count, idempotent guard, and (manual) cascade. (/luis 2026-06-17)
- [x] Stage 2 — `lib/utils/scenarioMembership.ts` `resolveScenarioEvents` + composite `mergeOverride` + unit tests — DONE 2026-06-17. Pure resolver (drops absent/disabled, applies override; returns pool refs unchanged when no override → cache-friendly). `mergeOverride` shallow-merges atomics, deep-merges composite wizardInputs, and re-runs the per-type pure calculator (home/child/car/retirement/state-move/mortgage-refi) to rebuild generatedEvents on an inputs/date change, stripping stale createdAccountIds; CUSTOM/JOB_CHANGE/WEDDING/COAST_FIRE fall back to a date-shift (no pure generator). 20 unit tests. (/luis 2026-06-17)
- [x] Stage 3 — API/repo/client/guest — DONE 2026-06-17. New `app/api/event-membership` (GET by scenario/event/all, scoped via scenarios.userId join; merge-upsert POST that never clobbers override-vs-enabled; DELETE). `scenario-events` GET now returns authoring-home ∪ membership superset; POST seeds an enabled cell (+ optional draft Base cell); DELETE + scenarios DELETE clean up cells manually (FKs are OFF). Onboarding-submit + guest-migrate bulk paths seed cells. `eventMembershipApi`/`eventMembershipRepo` + `guestEventMembershipApi` (synthesizes 1:1 cells for cell-less guest events). SQL smoke-tested. (/luis 2026-06-17)
- [x] Stage 4 — read-path swap + **regression gate** — DONE 2026-06-17. `fetchResolvedEvents(scenarioId)` (pool GET + cells GET → `resolveMembership`) replaces all 4 base-event load sites + saved-comparison hydration in page.tsx. `resolveMembership` safety net: an authoring-home event with no cell defaults to enabled (never vanishes). Byte-identical on backfilled data (returns same event refs). Regression gate GREEN: full suite **891 tests pass** + `tsc` clean + production build compiles (route registered). (/luis 2026-06-17)
- [>] Stage 5 — UI (PARTIAL, built blind 2026-06-18, tsc/jest/build-verified but NOT visually verified). DONE: (1) **Event Matrix modal** (`app/financial-planner/components/EventMatrixModal.tsx` + pure `lib/utils/eventMatrix.ts`) — checkbox grid, rows = events deduped by id grouped Shared/Differs, columns = Base+comparisons, per-scenario override chips; checking a comparison cell adds/removes the event in-memory, Base cell promotes/demotes against the live plan (`scenarioEventsRepo`). Wired via a "Manage events across scenarios" button in compare mode. (2) **Swim-lane timeline** (`SwimLaneTimeline.tsx` + pure `lib/utils/swimLane.ts`) — lane per scenario, date-positioned markers, click → opens matrix. 16 helper tests + 4 render-smoke tests. Clone-carry path RETIRED in Stage 6. REMAINING (the only Screen-3 gap): `TimelineDrawer` header scenario-switcher + grayed not-applied rows + "+ Add to <scenario>" — redundant with the matrix for the core capability (per-scenario editing already works via the chip calendar), so optional polish. **Live compare still uses the in-memory model (events matched by id), NOT a membership-cell rewrite** — the matrix mutates `comparisonScenarios[].events`; saved scenarios resolve from DB cells. Needs a visual pass against the mockup. (/luis 2026-06-17)
- [x] Stage 6 — DONE 2026-06-18 (tsc/jest/build-verified, NOT visually verified). (1) **Membership-based save**: `handleRequestSaveScenarios` no longer clones every event — a saved comparison scenario references Base's events via `event_scenario_membership` cells (with a `scenarioOverride` patch where its copy diverges, incl. hidden via `divergesForSave`); only comparison-only events (absent from Base) are persisted as rows. Round-trips through Stage 4's `fetchResolvedEvents` (test: `scenarioOverride`→`mergeOverride` restores the copy). (2) **Retired the clone-carry path**: removed `handleAddComparisonEventFromLibrary` + the ScenarioManagerBar "From other scenarios" library (superseded by the Event Matrix); stopped writing `originEventId` (page.tsx carry handler + scenario-events POST persist); `eventLineageKey`/`originEventId` kept read-tolerant + `@deprecated`. (/luis 2026-06-17)
- [x] Scenario Compare — visual QA pass on the Event Matrix modal + swim-lane timeline vs the Luis mockup — DONE 2026-06-19. Rendered live in Compare mode (Base + 2 comparison scenarios). Matrix modal: header/subtitle, group dividers (Shared/Differs), check/dashed-+ cells, per-column net-worth metric, footer legend all render correctly and match the mockup. Swim-lane: lanes, labels, date-positioned markers, year ticks all render. Base-removal confirm dialog markup verified in code (well-formed; not triggered live to avoid mutating the real plan). Findings split into the items below; fixed in-pass: duplicate "Scenario 2" name collision when adding a what-if alongside a hydrated saved "Scenario 2" (handleAddComparisonScenario now skips names already in use — page.tsx). (/qa 2026-06-19)
- [x] Scenario Compare — Event Matrix override chips show "modified" for composites without a homePrice — DONE 2026-06-19. `summarizeOverride` (lib/utils/eventMatrix.ts) now surfaces a per-type headline value for every composite: JOB_CHANGE → "$145K salary", RETIREMENT → "−20% expenses"/"Retire", WEDDING → "$30K wedding", CAR_PURCHASE → price, MORTGAGE_REFINANCE → "5.5% rate", STATE_MOVE → "→ TX", CHILD_BIRTH → "$1.5K/mo childcare", COAST_FIRE → "$500K target"; CUSTOM + missing inputs still fall back to "modified". 2 unit tests added (9 type assertions). Verified jest + tsc. (/qa 2026-06-18)
- [x] 🔴 Scenario Compare — Event Matrix shows every event TWICE when a saved comparison scenario is loaded — DONE 2026-06-19. ROOT CAUSE confirmed against the dev DB: a hydrated saved scenario carries its own event copies with different ids than the live base pool (legacy clone-model data — 19 events authored on the row, zero membership cells, no `originEventId`), and the matrix deduped rows by raw `id`, so each shared event listed twice. FIX: `buildEventMatrix` (lib/utils/eventMatrix.ts) now groups rows by a content `lineageKey` (`tier|type|name|date`) instead of raw id, so copies of the same logical event collapse to one row regardless of id provenance (legacy saves, clones, future membership data) while genuinely-distinct events stay separate. Each `MatrixCellView` now carries a per-column `eventId` (the column's own copy when present, else the canonical id to clone/promote) and the modal toggles on `cell.eventId`, so removal/promote still operate on the right event — no change needed to `handleMatrixToggle`. A scenario `amount`/`wizardInputs` override still groups (key-invariant) and surfaces as a chip instead of a duplicate row. 3 unit tests added (collapse-across-ids, canonical-id-on-add, keep-distinct). Verified jest (50) + tsc + LIVE in browser: the doubled 16-row matrix is now 9 rows (7 Shared-across-all + 2 genuinely Scenario-2-only home purchases). (/qa 2026-06-19)
- [x] Scenario Compare — swim-lane marker crowding — DONE 2026-06-19. `buildSwimLanes` now positions each event ONCE on the shared union (not per-lane) and de-overlaps with MIN_GAP bumped 3.5→6% (≈ the 26px marker's track width), so a dense same-date cluster fans out left→right instead of stacking into a blob. Positioning once also fixed a latent bug — the same event previously de-overlapped to different x in different lanes (misaligned). Verified live: the former 2027 blob is now evenly spread. (/qa 2026-06-19)
- [x] Scenario Compare — swim-lane "ghost" markers — DONE 2026-06-19. `buildSwimLanes` now renders the full event union in every lane: solid where the lane carries the event, a dimmed (opacity .32) + dashed "ghost" where it doesn't, vertically aligned with the lane(s) that do — so divergence is scannable on the lane itself (matches the mockup `.marker.ghost`). Events are grouped by the shared `lineageKey` (exported from eventMatrix.ts) so different-id copies of one logical event collapse to a single aligned column. Clicking a ghost opens the matrix to add it (aria/title say "not in <lane>; click to add"). `SwimLaneTimeline.tsx` renders present/ghost; 3 unit tests added. Verified live: Base shows 2 dashed ghosts aligned above Scenario 2's two solid home-purchase markers. (/qa 2026-06-19)
- [x] Scenario Compare — Event Matrix narrow/mobile overflow — DONE 2026-06-19. `EventMatrixModal.tsx`: name column `w-[48%]`→`w-[40%] min-w-[150px]`, scenario columns gained `min-w-[92px]`, and the column-header label is now `whitespace-nowrap` (+ `flex-none` dot) so "Scenario 2" can never break mid-word to "Scenari / 2" again; when columns can't all fit the modal body's existing `overflow-auto` scrolls horizontally with readable columns instead of squeezing. Verified the header-wrap fix live (one line, no regression at desktop width); the narrow-scroll behavior is by construction — live <500px re-verification was blocked by browser-automation viewport emulation not reflecting window resizes (same limitation noted in the Jun 2026 mobile pass). (/qa 2026-06-19)

### Current Plan header redesign — switcher-title (Luis, 2026-06-22)
Design approved. Spec + visual contract: `design-mockups/luis/current-plan-header/` (`SPEC.md` = engineering
spec, `option-B-switcher-title.html` = mockup, `index.html` = 3-direction comparison). The drawer header crams
four jobs into one un-wrapping row (identity, an ambiguous pencil "Manage Plans", the new "Compare to US avg"
view toggle, Save/close) → crowds on desktop, overflows at 390px. Fix: the **title becomes the plan switcher**
(`Current Plan ⌄` opens the existing version popover), date+status move to a subtitle, and the view toggle gets
a deliberate home (inline-right on desktop, full-width strip on mobile). **All changes in
`app/financial-planner/components/CurrentPlanDrawer.tsx`.** **Contracts:** the consolidated popover behaves
identically (only its trigger moves); the benchmark toggle stays ONE node portaled from PlanEditor
(`page.tsx:2130`) — repositioned by CSS, never duplicated; all header states preserved (current/archived/frozen/
review/new). Regression gate is visual: tsc clean + every state renders in dark+light at 390px (no engine code →
no financial suites). Build in stages (see SPEC.md):
- [x] Stage 1 — restructure header markup into switcher-title + subtitle + right cluster; wire title → `openPopover` (popover JSX reused verbatim, re-anchored under the title). (/luis 2026-06-22) — DONE 2026-06-22. `CurrentPlanDrawer.tsx` header rebuilt: the title is now a single switcher `<button>` (`Current Plan ⌄` / `Archived Plan ⌄`) showing title-line + rotating chevron, with status dot + label + `Effective <date>` as a subtitle beneath; clicking it drives the existing `openPopover`/`setPopoverOpen` logic and the consolidated popover JSX is reused verbatim, re-anchored `absolute top-full left-0` under the title (now wrapped by `popoverRef`). Switcher affordance gates on `showSwitcher = isExistingPlan && !reviewMode`; new/duplicate/review render a static title + editable `DateFieldInput` in the subtitle slot. The `View Only`/`Read Only` lock badge folded into the subtitle. New derived helpers: `showSwitcher`/`titleText`/`subtitleStatus`/`lockBadge`. Verified tsc + live (popover opens anchored under title, chevron rotates, all sections intact).
- [x] Stage 2 — benchmark toggle: reposition the single portaled node (inline-right desktop / full-width strip mobile via flex-wrap+order), optional quiet-pill wrap. (/luis 2026-06-22) — DONE 2026-06-22. Header is now `flex flex-wrap`; the `benchmarkTogglePortalRef` target node moved out of the title block into the right region via CSS only (ONE portal, no duplication): desktop `sm:order-2 sm:w-auto` as a quiet pill (`sm:border sm:rounded-full bg-theme-surface-2`) left of Save; mobile `order-last w-full` full-bleed strip below the header (`-mx-3 border-t`), with `[&>button]:w-full [&>button]:justify-between` so the label/switch span the strip. `empty:hidden` collapses the node (and a `peer-empty`-gated thin divider before Save) when no benchmark is available. Verified live: toggle repositioned + still drives `showBenchmarks` (single portal intact, no double-fire).
- [x] Stage 3 — state variants: new/duplicate/review (editable date in subtitle slot, no switcher) + archived (Update Plan) / frozen (Upgrade to edit) right-cluster buttons in the new layout. (/luis 2026-06-22) — DONE 2026-06-22. All states preserved by the conditional structure: current → switcher + Save; archived → switcher + `Read Only` badge in subtitle + right-cluster `Update Plan` (verified live); new/duplicate → static `New Plan` + editable date; review → static `Review Updated Plan` + `from: <event>` + editable date; frozen → `View Only` badge + `Upgrade to edit`. Right-cluster branching (Save/Update/Upgrade + close) kept verbatim, only re-ordered. Verified tsc.
- [x] Stage 4 — QA pass: dark + warm-ivory, 390px, every header state; confirm popover open/close, version switch, toggle lights body pills, Save/Update/Upgrade fire. (/luis 2026-06-22) — DONE 2026-06-22. tsc clean, no console errors. Live-verified in browser: **dark** (current state — switcher-title, green-dot subtitle, toggle pill left of Save) + **warm-ivory light** (same, quiet pill reads correctly); popover opens anchored under the title with chevron rotation + version switch; benchmark toggle repositioned and functional; **archived** state confirmed via text extraction (title `Archived Plan`, `Read Only` badge, right-cluster `Update Plan`). NOT live-verified (env-limited, structurally covered): 390px mobile reflow (browser-automation viewport emulation doesn't reflect resizes — same limitation as the Jun 2026 mobile pass; the `order-last w-full` strip is by-construction), frozen `Upgrade to edit` (needs a frozen account), review-mode (needs the apply-to-plan flow). Renderer froze on some screenshots (known dev-env quirk) — used get_page_text fallback.

### QA sweep (Jun 2026) — Plan Timeline + projection horizons
Source: /qa walkthrough — onboarding → Plan Timeline → create future event → confirm projections at 5Y / 35Y. The create-event → projection-update → delete flow works (verified live: a Set-Salary $250K event moved terminal NW $11.6M→$14.1M and pulled FI 2038→2036; delete reverted everything cleanly). Projections read sensibly across the horizon (5Y ≈ $1.7M; 35Y/2061 ≈ $16M on the clean plan; Full/2086 ≈ $85.9M). Onboarding itself redirects to /financial-planner for an already-onboarded account (expected) — the fresh wizard wasn't exercised. Functional/age bugs filed under Bugs / Issues; visual-label nits below.
- [x] Net-worth chart X-axis duplicates the first year label — at 5Y the first two ticks are both "2026" (chart start + end-of-first-year both render as 2026). (/qa 2026-06-22) — DONE 2026-06-23. The "Current" point (sentinel `currentYear-1`, labeled `currentYear` in the strip gutter) and the projected end-of-current-year point both carry the current year. ChartAxisStrips now suppresses any annual-mode per-point label whose calendar year equals the gutter's `firstCalYear` (applied to the Year strip + both age strips), so the start year shows once. Verified tsc + production build.
- [x] Timeframe vs impact-horizon year label off by one — the 30Y view's last X-axis tick reads "2055" while the timeline's per-row impact column reads "by 2056" (30Y from 2026 = 2056). Align the chart's terminal-year label with the impact horizon. (/qa 2026-06-22) — DONE 2026-06-23. The annually-sampled series ends at `currentYear + projectionYears - 1` (2055 at 30Y), but the timeline got `projectionEndYear = currentYear + projectionYears` (2056), so "by 2056" mismatched the 2055 tick (and the delta `.find` missed, relying on a fallback). page.tsx now derives `impactHorizonYear` from the chart's actual terminal year (`netWorthData[last].year`) and passes it to both TimelineDrawer instances, so the "by &lt;year&gt;" label matches the last tick and the per-event delta lookup lands on a real point. Verified tsc + production build.
- [x] Born-milestone age suffix inconsistent — "Keegan Born" carries an age suffix (currently "· Age -1", the bug filed separately) while "Baby #2 Born" carries none (the future child isn't a household member yet, so no birthDate → no age). Render born-milestone age suffixes consistently across milestones. (/qa 2026-06-22) — DONE 2026-06-23. BABY_BORN milestones now render a uniform "· Newborn" suffix (age 0 by definition) regardless of whether the child is a household member yet, so "Keegan Born" and "Baby #2 Born" read the same. Non-born milestone ages use the new `ageInYear` helper and are suppressed when negative. Verified tsc + jest + production build.

## Security

## Engineering (New User Experience)
Source: account-onboarding UX review (design-mockups/account-onboarding-ux-review/). Outstanding observations split out from the now-closed "Consult Claude on account-adding UX" item.
- [x] Theme consistency: onboarding rendered dark while the in-app Accounts drawer rendered light in the same session (review Obs #3) — AUDIT: the cross-surface divergence is already resolved — theme is fully global (single `data-theme` on `<html>`, set only by the layout.tsx anti-flash script + ThemeContext; no per-route overrides; onboarding/page.tsx documents this), and the accounts surface uses CSS-var token classes (no hardcoded light/dark colors — the only literals are theme-neutral `bg-black/50` modal scrims). The historical "Accounts drawer light" was a since-fixed theme-state bug. FIXED a residual: ThemeContext seeded React state to DEFAULT_THEME and reconciled the persisted preference in a `useEffect`, so a light-mode user's first render produced dark `themeConfig` (chart line `#34d399` vs `#10b981`, gradient opacity) until after paint — a JS-vs-CSS flash. Moved reconciliation to an isomorphic layout effect that reads the already-resolved `data-theme` (falling back to localStorage), so chart colors match the CSS theme before first paint; kept the initial render at DEFAULT_THEME to match SSR markup (avoids a ThemePicker hydration mismatch). Verified tsc + production build
- [x] Auto-skip the owner step for single-owner households — AccountSetupWizard now derives `singleOwner` (ownerOptions.length === 1) and collapses the owner-column matrix when there's one member with no Joint option: the per-owner header dots/legend, the "ALL / toggle-for-everyone" row affordance, and the cart's per-owner group headers are hidden, and each account type renders as a single add toggle (desktop grid + mobile rows + balances sheet). Multi-owner households are untouched — every change is guarded on `singleOwner`. Verified tsc + production build (Obs #9)
- [~] ~~Progressive disclosure for rarer account types in onboarding (Obs #8)~~ — DROPPED 2026-06-16. (Was: all types are always-visible rows, only "I own my home" gated; add a "+ more account types" collapse.)
- [x] Credit-card editor simple/advanced split — balance, allocation %, utilization, payoff math all show at once; overwhelming as a first impression. Default to a simple view, fold the rest into "advanced" (Obs #12) — DONE 2026-06-19. Advanced fields were already in CreditCardDetailPopover, but the inline row still showed Balance + Min Pay + Next Payment + Payment Date per card plus the allocation-summary bar. Added a remembered Simple/Advanced segmented toggle (shared `SegmentedToggle`) to CreditCardEditor, default **Simple**, persisted in localStorage (`planyfi.ccEditorMode`); Simple mode hides the allocation metrics ("Plan CC Expenses" + "% allocated") and collapses each card row to just Balance (+ name/owner/details/remove). Added Min Payment / Next Payment / Payment Date inputs to the popover so they stay editable in Simple mode (popover is now the complete advanced surface). Moved shared `getDefaultPaymentDate` into holdingsUtils to avoid a CreditCardItem↔popover circular import. Verified tsc + jest (holdingsUtils 6/6); production build skipped (active dev server holds .next lock).
- [x] Reduce account-row icon density — DONE 2026-06-21. Added a self-contained `RowActionMenu` (kebab ⋮ → "Edit account" / "Delete") to `AccountRow.tsx`, mirroring the existing `AddHoldingMenu` pattern (own open/ref state + outside-click/touch handlers, `aria-haspopup`/`role="menu"`/`menuitem`); replaced the inline ✏️/🗑 buttons in BOTH the mobile and desktop icon rows. The 🔗 budget-link popover (shows linked/unlinked state) and the contextual `+` add-holding menu (primary action for empty asset accounts) stay inline. Per row: `🔗 ✏️ 🗑` → `🔗 ⋮`. Verified tsc. (Obs #10)
- [~] ~~Strengthen holdings reconciliation signal — delta is colored in the editor footer but easy to miss; consider an account-row-level badge when holdings ≠ balance (Obs #11)~~ — DROPPED 2026-06-21. Redundant by design: saving a holdings account always rewrites its recorded balance to the holdings sum (`useAccountHoldings.ts:337` → `balanceHistoryApi.add({ amount: sum })`), so holdings can never *persistently* disagree with the recorded balance. The only remaining diffs are unsaved edits (already flagged by the dirty dot) or live stock/crypto market movement (already shown in the row's change column) — a badge would add noise, not clarity. The one genuinely non-noisy reconciliation gap (allocation-mode account whose target %s ≠ 100%) is already surfaced in the expanded editor's "Allocated X% (Y% left)" line.
- [x] Simplify the Demographics percentile table on desktop — information-dense and visually competing (Obs #5); mobile already has a stacked fallback — DONE 2026-06-22. The desktop view defaulted to a full 4-column × 9-row matrix competing with the actual choice (which income card). Now the income cards carry the decision and the line-by-line breakdown is collapsed behind a "See typical spending & net worth" disclosure (default collapsed, chevron rotates on expand) — first paint is just the 4 clean percentile cards. To keep signal when collapsed, each `PercentileCards` card gained a muted "<abbrev> net worth" subtitle. Extracted a shared `estimatedNetWorth(b)` helper in onboarding-benchmarks.ts (used by both the card subtitle and the Est. Net Worth breakdown row — single source of truth, replacing the inline reduce in ROWS). Mobile stacked layout untouched (already shows the full breakdown per card). Verified tsc + production build. (Visual click-through recommended — dev-env renderer quirk makes live browser verification flaky.)
- [ ] Decide the "Your Numbers" rail on the Demographics step — currently excluded there (appears from Household onward); confirm intent or surface the FI consequence of the percentile choice (Obs #6)
- [ ] Revisit the Full onboarding path — "Enter full details (~10 min)" reads as a chore; decide whether to keep, simplify, or deprecate it (Obs #7)

## Bugs / Issues
- [x] Hover tooltip cut off on the right edge — DONE 2026-06-22, confirmed live by owner. The shared `app/components/ui/Tooltip.tsx` positioned its bubble `absolute` + centered inside the trigger (`left-1/2 -translate-x-1/2`), so a long (≤280px) tooltip on a right-leaning trigger (e.g. the benchmark comparison badges in the plan editor) overflowed and got clipped by the drawer's `overflow`. FIX: rewrote Tooltip to portal the bubble to `<body>` with `position: fixed` computed from the trigger's `getBoundingClientRect()` and clamped to the viewport (`left = max(8, min(left, vw − w − 8))`, same for top) — mirrors the portal + clamp pattern already used by the shared Select. Escapes all ancestor `overflow`; edge-adjacent triggers (any side) stay on-screen. Same props/styling, so every Tooltip consumer benefits with no caller changes. Repositions on scroll/resize. Verified tsc.
- [x] 🟠 Sign-up dead-end for existing accounts — DONE 2026-06-20, verified live in prod. A returning user who entered a registered email on `/sign-up` got a "that email is taken" error with no path to sign in. First tried passing `signInUrl`/`signUpUrl` props to Clerk's `<SignUp>`/`<SignIn>` (commit `43b4795`) — but Clerk's built-in "Already have an account? Sign in" footer link does NOT render in this instance's configuration, so that alone showed nothing (confirmed live by the owner). FINAL FIX (commit `7f2ae6e`): render our OWN explicit `<Link>` ("Already have an account? Sign in" / inverse) below the Clerk component on both auth pages — server-rendered in our page code, so it shows for every visitor regardless of auth state or Clerk config. Kept the `signInUrl`/`signUpUrl` props for Clerk's internal redirects. Verified tsc + deployed + confirmed both links present in prod SSR HTML. (Found 2026-06-19 verifying the prod deploy.) NOTE: the Dockerfile builder stage only bakes `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, not the other `NEXT_PUBLIC_CLERK_*` URL vars — not the cause here (props/explicit link sidestep it), but worth declaring those ARGs eventually for correct after-sign-in/up redirects.
- [x] Credit card UI broken/poor on mobile; replace with "Add credit card" flow similar to adding holdings, then prompt for details
- [x] Home Purchase event summary renders `"$NaN home, undefined% down, undefined% rate, 3% appreciation"` in the Future Events drawer card. Seen on a fresh Quick Setup demo plan (seeded HOME_PURCHASE event) — home price / down payment / rate fields come through undefined when the event is auto-seeded rather than user-entered. Likely the seeded composite payload is missing fields the card summary reads, or the summary doesn't guard against undefined.
- [x] Fix input boxes for fixed and discretionary expenses on mobile — PlanRow's mobile Row 2 is a flex row, but the expense/investment item-wrapper was a plain `flex flex-col` that collapsed to content width, shrinking the `flex-1` amount input. Made both wrappers `flex-1 min-w-0` so the input fills the row on mobile (desktop keeps `sm:w-28`). Verified desktop non-regression + mobile-context DOM sandbox
- [x] Fund Strategy surplus routing can exceed a tax-status cap when a later account-group bucket also matches the capped account — added a claim mechanism to routeSurplus mirroring withdrawByPriority: capped buckets claim their accounts so later broad buckets skip them (Roth no longer funded past the $7k limit by the Investments bucket). All 166 allocation/projection/irs-limit tests pass. Default config: Roth IRA is funded by both the "Tax-Free / $7k annual limit" bucket AND the broader "Investments" bucket, so routeSurplus contributes beyond the IRS limit. Decide whether group buckets should exclude accounts already capped/claimed by an earlier bucket (engine: `lib/utils/allocation-engine.ts` routeSurplus has no claim mechanism, unlike withdrawByPriority).
- [x] Adding holding/ticker on mobile: move search bar to bottom with matched results displayed above it — SecuritySearch now elevates into a mobile bottom sheet (results scroll above, search input + Done pinned at bottom, focus moves to it on open). Desktop dropdown unchanged. Note: mobile sheet verified by code review + tsc only — browser automation can't emulate the <768px viewport to exercise it live
- [x] Holdings overflow horizontally on mobile when values have too many digits — the metrics block (Qty/Price/Value/%/icons) was a single nowrap shrink-0 flex row; long values pushed it past the card width. Made it `flex-wrap` (right-aligned) on mobile, `sm:flex-nowrap` on desktop. Verified via mobile-width DOM sandbox (before: 59px left overflow; after: fits, wraps to 2nd line)
- [x] Font, icons, and UI elements too small on mobile — explore improvements without full redesign. RESOLVED (Jun 2026): global mobile legibility floor — a single max-width:639px block in globals.css raises the smallest text utilities (text-xs→13px, text-[10px]→12px, text-[11px]→12.5px) across all ~950 occurrences; ICON_BTN_BASE touch target → 36px on mobile (30px at sm:); AccountsBalanceSheet balance column 70→80px so 7-figure net worth doesn't clip. Desktop (≥640px) unchanged. Verified tsc + production build + CSS cascade against built output. Manual <375px visual sweep recommended.
- [x] Plan investment/savings categories should auto-link after being added in the previous onboarding step — handleAddInvestmentCategory now uses the shared suggestAccountLinks heuristic (type/owner/tax/name) instead of an exact string match; verified in full-onboarding plan editor (Brokerage→Brokerage, Roth IRA→Roth IRA, no tax mislink)
- [x] Accounts drawer "Updated -1 days ago" — balanceFreshnessLabel/getBalanceFreshness now compute the day diff on a shared UTC calendar-day basis (saved dates are toISOString-stamped in UTC; they were compared against LOCAL midnight, off-by-one in TZs behind UTC) and clamp future-dated saves to 0. Handles full ISO timestamps too. 6 unit tests added
- [x] /rent-vs-buy: fixed PlanyFI wordmark overlaps banner/content — LayoutShell now reserves top space (pt-16) on pages that render the fixed wordmark/bell (showBell), so the session banner and page content clear the fixed logo on every width instead of sliding under it. Verified: tsc + production build (visual click-through recommended)
- [x] Guest "Saved for this session" toast stacks twice after onboarding skip — useUpgradeNudge now rate-limits the nudge by TIME (first save, then at most once per 30s) instead of by save count (1st/every-5th), so a burst of back-to-back guest writes coalesces into a single toast. Verified: tsc + production build
- [x] Tune guest beforeunload warning — the warning no longer treats onboardingCompleted as data; it now requires real content (accounts, or a budget with line items / income). Skipping onboarding with an empty plan no longer triggers the misleading "data will be lost" prompt. Verified: tsc + production build
- [x] 🔴 Compare-mode delete destroys REAL accounts — gated both handleDeleteEvent and handleDeleteWithAccounts on the existing skipAccountSideEffects flag (TimelineDrawer). In Compare mode (in-memory eventsStore) deleting a cloned composite now just drops the event from the scenario and never opens the account-delete dialog or calls deleteAccountsByEvent/unlinkAccountsFromEvent against the user's real accounts. Kept createdAccountIds on the clone so the compare projection still reuses the existing real accounts (matches Base; the gate is the safety net). Verified: tsc + production build
- [x] 🔴 ADDED_BY_EVENT guard blocks ALL cross-event category-only removes — replaced the dead atom-id carve-out with serializable composite-level ownership. expandEvents() now stamps each atom with sourceEventId/sourceEventName (composite id for generated atoms, own id for standalone atomics); applyAtomicEvent resolves ownerEventId from context ?? event.sourceEventId and tags added lines + the category-only remove guard with it. A composite's own category-remove now removes its own added line (previously dead), while a DIFFERENT event's lines are preserved (sibling test still passes). Added projectPlan tests for same-event removal. 252 financial/scenario tests pass
- [x] 🔴 ADDED_BY_EVENT is a Symbol and JSON-serialization strips it — same fix: ownership is now the serializable sourceEventId line-item field instead of a Symbol, so it survives applyEventToBudget's JSON.parse(JSON.stringify(basePlan)) clone. Added a test proving a chained apply (home A's mortgage added, JSON round-trip, home B's category-only remove) preserves home A's line
- [x] 🔴 Comparison save flow is non-atomic delete-then-reinsert — reordered handleRequestSaveScenarios to insert the new generation FIRST (with rollback of partial inserts on error), then delete the old isComparison rows only after every new row + its events are durably written. A mid-save failure or tab close now leaves the previously-saved scenarios intact instead of destroying them; a failed cleanup leaves only harmless duplicates the next save reconciles. (Full single-transaction atomicity via a server endpoint is a possible follow-up.) Verified: tsc + production build
- [x] Base-scenario fallback can promote a comparison row to base — fixed to `find(s => !s.isComparison) ?? allScenarios[0]` so a saved what-if row is never self-healed into the base (which would swap the live plan to what-if events). Verified: tsc + production build
- [x] expandEvents expense disambiguation — two fixes: (1) disambiguation now runs in DATE order (registry.ts sorts events by date before assigning clean/suffixed labels) so the earliest composite deterministically keeps the clean label regardless of array order; (2) EXPENSE_CHANGE update/remove now match by linkedAccountId first when the event targets an account a line is bound to (projectPlan.ts matchesEvent), so MORTGAGE_REFINANCE on home B updates B's mortgage by account instead of overwriting home A's clean-labeled line. Tests added for both. RESIDUAL: "Remove PMI" carries no linkedAccountId (home-purchase PMI line isn't account-linked), so removing the 2nd home's PMI still relies on label matching — left as a follow-up since there's no precise key linking a refinance to a specific home's PMI line
- [x] Custom add-account impacts silently dropped in Compare mode — added pure `syntheticAccountsFromImpacts(event)` (syntheticAccountStubs.ts) that derives synthetic accounts from a custom event's `add-account` impacts when no real accounts exist (Compare mode). Wired into scenarioProjectionCore §10b (engine now projects the inheritance/windfall/loan balance) AND withSyntheticAccountStubs (display panels resolve the name). Guarded on `!createdAccountIds` so normal mode is untouched. 5 unit tests added
- [x] Compare + monthly resolution mis-decodes the selected year — toggling Compare now resets resolution to 'annual' and selectedYear to null (handleCompareModeChange in page.tsx), so a leftover monthly composite key can't be mis-decoded by selectedCalendarYear's decodeMonthKey into a bogus year. Verified: tsc + production build (visual click-through recommended)
- [x] ComparisonTable renders all "—" after clicking the "Current" bar — two-part fix: (1) toggling Compare resets selectedYear (above); (2) ComparisonTable now clamps displayYear into the projected non-current [minYear, maxYear] range, so a stale currentYear-1 sentinel no longer sits below every point and dashes the table. Verified: tsc + production build (visual click-through recommended)
- [x] 🔴 Compare: scenario income events barely affect net worth — ROOT CAUSE: not Compare-specific. routeSurplus deposited extra surplus into IRS-limited accounts (401k/Roth), then the engine's post-hoc IRS clawback deleted it from balances WITHOUT re-routing it anywhere (and surplusBucketAmounts was recorded pre-clawback, so the rail claimed it was allocated). For Quick Setup users (no brokerage, capped cash bucket) nearly all marginal income evaporated. FIX: routeSurplus now takes per-limit-group deposit rooms and honors IRS limits at routing time (excess cascades to later buckets); surplus no bucket can absorb falls back into cash accounts (`__cash-overflow__` pseudo-bucket — real money doesn't evaporate); IRS-trimmed linked contributions are returned to surplus; scenarioProjectionCore defaults the allocation strategy (undefined strategy silently dropped ALL surplus). Repro test: +$20K×2yr event now moves terminal NW +$33K (was +$1.5K). 847 tests pass. NOTE: base projections rise for capped users — previous numbers understated NW (/qa Jun 2026)
- [x] Onboarding savings slider doesn't match the resulting plan — handleComplete now passes `monthlySavingsTarget` (the exact $/mo the Expenses step displayed, take-home × rate); buildBudget scales the benchmark 401k/Roth/emergency mix to that total (slider honored) and the scratch path uses it directly (fixing its gross-basis math). Prefill also derives the slider's starting % from the benchmark's actual contribution dollars ÷ estimated take-home, so slider/rail/plan agree from first render (the benchmark's published savingsRate didn't match its own contributions). Note: planner hero remains % of GROSS by definition — dollars now match; % differs only by basis (/qa Jun 2026)
- [x] FI year inconsistency — rail's calculateOnboardingFI was compounding only the slider's savings $ while the planner engine accumulates the FULL surplus (take-home − expenses) into accounts; YourNumbersRail.useFI now uses (monthlyNet − monthlyExpenses)×12 as annualSavings, matching the planner's accumulation model (same 25× target both sides) (/qa Jun 2026)
- [x] Planner first-run tour — (1) overlap: above-placement used a hardcoded 140px height estimate; popover now anchors its bottom edge via translateY(-100%) on a wrapper (animation moved to inner div since fade-in-up also animates transform) so the real rendered height drives placement; (2) outside click no longer ends the tour (backdrop inert; Skip / Got it / Escape are the only exits — Escape handler added) (/qa Jun 2026)
- [x] Investment and savings contributions still not auto-linking in detailed onboarding — ROOT CAUSE: the PlanEditor wizard-prefill effect (app/plan/edit/page.tsx) read + `removeItem`'d sessionStorage `planWizardData` on its first run while `accounts` was still loading async (empty `[]`), so the account-derived investment/debt line items (which carry `linkedAccountId` + per-owner assignment) were skipped and the payload was consumed before it could ever apply — leaving the unlinked, individual1-only defaults. Added an `accountsLoaded` flag (set in the accounts fetch's `.finally`) and gated the prefill effect on it so the payload is consumed only after accounts resolve. Verified tsc + production build (31 onboarding-builder tests pass; in-app click-through recommended)
- [x] When multiple household members have the same account type, only one person's accounts are carried into the plan — SAME root cause as the auto-link fix above: the prefill race left the individual1-only defaults, so a partner's same-type account produced no contribution at all. With the `accountsLoaded` gate the prefill now maps every account to its own line item (correct owner + link), so e.g. both partners' 401(k)/Roth IRA accounts each get their own contribution row (rendered as a per-owner split). Verified tsc + production build
- [x] Credit Card and Auto Loan accounts show "N/A" for tax status — replace with blank or a simple label like "Debt" — DONE 2026-06-22. ROOT CAUSE: the AccountSetupWizard cart-item subtitle rendered `{meta.tax}` straight from `typeMeta()`, which returned the type's default `taxStatus` ('N/A' for all debt types). (The account-row, add-form, and edit-modal surfaces already hide tax status for liabilities / gate it behind Custom type — the wizard was the only leak.) FIX: `typeMeta` now returns `tax: isLiability ? 'Debt' : d.taxStatus`, so Credit Card / Auto Loan (and every liability type) show "Debt" instead of "N/A". Verified tsc.
- [x] No input box shown when fixed/discretionary expenses are collapsed — DONE 2026-06-22. CAUSE: "Collapse" = the section-level "Enter as single total" (aggregate) toggle. By deliberate anti-drift design, aggregate mode on a Fixed/Discretionary section that HAD itemized rows showed a READ-ONLY total ("matches itemized items") with no input; only empty sections got an editable lump-sum box. DECISION (owner, AskUserQuestion): make the single-total input editable & authoritative, items preserved. FIX (app/plan/edit/page.tsx, mirrors the existing Investments aggregate pattern so the projection engine is untouched): (1) removed the read-only branch so aggregate mode always renders the editable lump-sum input; (2) `useLumpSum` now keys off `aggregateMode` alone (was `&& !hasItems`) so the section subtotal reflects the typed total; (3) `totalMonthlyExpenses` excludes aggregate-type items from the itemized sum and adds the lump-sum (no double-count); (4) `handleSave` folds ANY aggregate-mode type into a synthetic `${type} Total` line item and drops its itemized rows (was gated to Investments-or-empty). Within a session the itemized rows stay in React state and reappear on switching back to itemized; saving while collapsed consolidates them into the total (same as Investments today). Verified tsc + jest (projectPlan/useScenarioNetWorth/allocation-engine, 179). NOTE: like Investments, a save-while-collapsed then reload-then-expand surfaces the synthetic "X Total" row (pre-existing aggregate quirk) — left at parity.
- [x] Financial Planner options overflowing on mobile with Compare button present — DONE 2026-06-22. CAUSE: ChartControlsBar Row 1 is `flex justify-between` with metric tabs (left) + lens group (right). On mobile the lens group collapses to just the Compare pill + gear (granularity/timeframe/export are all `hidden sm:`), but the left tabs row had no shrink/scroll, so the Compare pill pushed the row past the viewport (page-level horizontal overflow). FIX (ChartControlsBar.tsx): left tabs container → `min-w-0 flex-1 overflow-x-auto scrollbar-none` (tabs scroll within their space, matching the ComparisonTable convention); right lens group → `shrink-0 pl-2` so Compare/gear stay pinned and fully visible. No page overflow regardless of tab count. Verified tsc.
- [>] 🟠 FI status truncated by chart timeframe — selecting a shorter projection window (e.g. 5Y) flips the Financial Independence panel to "FI not yet reached" / "FI Date: Not reached" with an "increase contributions / adjust withdrawal rate" prompt, even when the plan reaches FI beyond that window (30Y and Full both correctly show "On track — FI 2038"; the QA plan reaches FI ~2036–2038). ROOT CAUSE: `calculateFireMetrics` (lib/utils/fire-calculator.ts:49-51) scans the `projectionPoints` it's handed for the first year netWorth ≥ FI number, and the caller feeds it the timeframe-truncated series, so FI past the visible window is never found → `fiYear=null` → ScenarioSummaryBox renders "not reached" (ScenarioSummaryBox.tsx:904,949). FIX: compute FI metrics over the full plan horizon (or one long enough to detect FI) independent of the chart's display timeframe — the FI panel must not change with the zoom dropdown. (/qa 2026-06-22) — DONE 2026-06-23. The FI panel now reads its metrics from a dedicated FULL-horizon net-worth series decoupled from the chart timeframe: page.tsx adds a second `useScenarioNetWorth` at `fiHorizonYears = Math.max(lifeExpectancyYears, 30)` — a value independent of `timeframe`, so it's computed once and stays cached as the user zooms — and `fireInputs` derives currentPoint / retirement-year point / projectionPoints from that `fiNetWorthData` instead of the chart-truncated `netWorthData`. Zooming to 5Y/10Y no longer flips FI to "not reached"; also fixes a latent miss where the retirement-year point lay beyond the display window. NOTE: ComparisonTable's per-scenario FI row (Compare mode) still scans display-truncated points — same class, left as a follow-up since it runs through `useComparisonProjections` at `projectionYears`. Verified tsc + jest (fire-calculator 15, age-calculator 12, +239 core projection/allocation tests) + production build. (Visual click-through recommended — dev-env renderer is flaky.)
- [>] Timeline milestone ages off by one / "Age -1" — the Plan Timeline shows "Keegan Born · Feb-2026 · Keegan · Age -1" (a birth milestone should read Age 0 / "Newborn"); other milestone rows read one year low (e.g. "Keegan Goes to College" on an 18th-birthday date shows Age 17; "Retirement (Mitch)" shows Age 54). The Add-Event date picker computes the same person's age correctly (Keegan 0 today / 4 in 2030), so the bug is in the timeline row only. ROOT CAUSE: TimelineDrawer.tsx:1236 `calculateAge(member.birthDate, new Date(event.date))` — `new Date('yyyy-mm-dd')` parses as UTC midnight (prior day in US timezones), and the event date can fall before the child's stored day-of-month, so calculateAge decrements (same UTC-parse class as the already-fixed "Updated -1 days ago" balance-freshness bug). FIX: parse event.date as a local date (or compare year/month only) and floor born-milestones to 0/"Newborn". (/qa 2026-06-22) — DONE 2026-06-23. TimelineDrawer now uses the new timezone-safe `ageInYear(birthDate, eventYear)` (calendar-year / year-subtraction, no UTC day-of-month math) instead of `calculateAge(birthDate, new Date(event.date))`, and BABY_BORN milestones render "· Newborn". Fixes "Keegan Goes to College" → Age 18 and "Retirement (Mitch)" → correct age. Part of the unified age-convention fix above. Verified tsc + jest + production build.
- [>] Age conventions disagree across views — for the same calendar year the net-worth chart's age axis and the Add-Event date picker show different ages (2030: chart axis "Mitch 37 / Kennedy 34" vs the date-picker preview "Mitch 36 / Kennedy 33"). The chart axis uses year-subtraction (getFullYear diff) while the picker/tooltip use exact-as-of-date age via `calculateAge`. Pick one convention so the same year never shows two ages. (/qa 2026-06-22) — DONE 2026-06-23. Standardized on ONE calendar-year convention (`calYear − birthYear`, timezone-safe) across the chart age axis/tooltip (already used it), the Plan Timeline, and the Add-Event date-picker preview — the only convention that guarantees a year never shows two ages. New shared helpers `ageInYear` / `birthYearFromString` (age-calculator.ts, 12 tests); DateInput's preview switched from exact `calculateAge` to `ageInYear`; page.tsx `primaryBirthYear`/`partnerBirthYear` now string-parsed (also fixes a latent Jan-1-birthday UTC off-by-one in the chart axis). Verified tsc + jest + production build.

## Parking Lot
- [ ] Scratch-pad what-if scrubbers — live sliders (retirement age, savings rate, return, home price) overlay a ghost projection on the chart; delta readout vs. saved plan; "Save as event" to persist [M effort, High leverage]
- [ ] Event impact sparklines — 180×40px sparkline per event card showing projection-with minus projection-without; magnitude number (+$412k at FI); left-border color by event type [M effort, High leverage]
- [ ] Confidence band — fan chart with p10/p50/p90; Phase 1: deterministic ±1.5σ; Phase 2: Monte Carlo [M effort, High leverage]
- [ ] Data export (JSON) for Premium — per-user server-side export endpoint — DEFERRED, not needed for beta
- [ ] Add CSV import error handling in TransactionsDrawer — catch malformed files and show user feedback — DEFERRED, low priority
- [ ] Zillow ZORI/ZHVI by ZIP code — download monthly CSV (~50MB), serve via API route for ZIP-level rent/home value benchmarks in onboarding and rent-vs-buy calculator [M effort, High leverage] — POST-BETA
- [ ] FRED API integration — pull live savings rate (PSAVERT), 30-yr mortgage rate, PCE inflation at runtime; cache daily with hardcoded fallbacks [M effort, Med leverage] — POST-BETA
- [ ] Census ACS API — ZIP-level income percentile comparisons ("you earn more than X% of households in your ZIP"); free REST API with caching [M effort, Med leverage] — POST-BETA
- [ ] SSA benefit formula — implement PIA calculation (90%/32%/15% of AIME at bend points) for Social Security income in retirement projections; bend points update annually [M effort, High leverage] — POST-BETA
- [ ] Debt & balance benchmarks (Phase 3 of the demographic benchmark buildout) — benchmark the NET-WORTH surface, not an expense line: show "typical for your age" bands/badges for debt balances (credit card, student, auto, mortgage) and retirement/investment balances. Data: Fed SCF + NY Fed Household Debt (profiles already cite SCF and carry `suggestedAccounts` balances to aggregate from). Build: a balance-benchmark resolver keyed by age (+ percentile) reusing the suggestedAccounts aggregation; render on the net-worth + accounts views. Context: Phase 1 (income + investment-contribution badges in the Current Plan editor) shipped 2026-06-22; Phase 2 (splitting the discretionary lump into CEX sub-categories) was DECLINED — total-discretionary comparison is sufficient. [M effort, Med leverage] — POST-BETA
